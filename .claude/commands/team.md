---
description: Show the agent team, what each role does, and which command to use
---
Read `docs/TEAM.md` and `ls .claude/agents .claude/commands`. Present the org chart in Thai as two short tables (roles → what they do → where they write; commands → flow), note which agent types are loaded in this session versus need a restart, and end with three example invocations relevant to `docs/product/BACKLOG.md`.
