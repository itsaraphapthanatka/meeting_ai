# meeting_ai — กติกาสำหรับคนและเอเจนต์ที่แก้โค้ดในที่เก็บนี้

ไฟล์นี้คือ **กฎที่ห้ามแหก** ฉบับสั้น ที่ถูกโหลดเข้าบริบททุกครั้ง
รายละเอียดเชิงสถาปัตยกรรม (แผนผังโมดูล เส้น API ตารางฐานข้อมูล สูตรทดสอบแบบแยกสภาพแวดล้อม)
อยู่ใน [docs/PROJECT-CONTEXT.md](docs/PROJECT-CONTEXT.md) — อ่านที่นั่นก่อนลงมือกับงานที่ไม่คุ้น
ของที่ยังค้างอยู่ทั้งหมดอยู่ใน [docs/product/BACKLOG.md](docs/product/BACKLOG.md)

---

## 1. ข้อจำกัดที่กำหนดรูปร่างของโค้ดนี้

- **`meeting_ai/` ใช้ stdlib เท่านั้น** ข้อยกเว้นเดียวคือ `psycopg` ซึ่งใช้ได้แค่ใน
  `web/db.py` กับ `web/pgstore.py` ส่วน `sherpa-onnx` เป็นของเสริมและถูก import แบบมีเงื่อนไข
  ใน `diarize.py` — หน้าเว็บก็ไม่มี npm ไม่มีขั้นตอน build
- **สองโหมดจากโค้ดชุดเดียว**: โหมดไฟล์ (`web/store.py`, ไม่มีล็อกอิน) กับโหมด cloud
  (`web/pgstore.py`) `web/backend.py` เลือกตอน import ฟังก์ชันของการประชุมต้องมีชื่อเหมือนกัน
  ทั้งสองฝั่ง ของที่มีเฉพาะฝั่ง cloud (auth/share/job/worker) ทุกจุดเรียกต้องมี
  `backend.cloud` / `backend.auth_required()` คุม
- **`runner.py` คือทางประมวลผลทางเดียว** ทั้งงานในโพรเซสและงานของ worker ไกล ๆ ใช้ตัวเดียวกัน
  อย่าแตกทางที่ผู้เรียกฝั่งใดฝั่งหนึ่ง
- **Vercel ไม่มี GPU และ function เพดาน 60 วินาที** งานหนักจึงอยู่ที่เครื่อง worker เสมอ และ
  body ของ Vercel จำกัด 4.5 MB เบราว์เซอร์จึงอัปไฟล์เสียงขึ้น R2 ตรง ๆ
- **Neon pooler เป็น transaction mode** → `prepare_threshold=None` และตารางต้องเขียนเต็ม
  `meeting_ai.<table>` เสมอ

## 2. ความปลอดภัยที่ต้องไม่พลาด

- ทุก endpoint ตรวจ **ทั้ง** การล็อกอินและความเป็นเจ้าของ/บทบาท เว้นแต่ตั้งใจให้สาธารณะ
  (แล้วต้องอยู่ใน `PUBLIC_API`) เส้นย่อยใหม่ของ `_meeting()` วาง **หลัง** บล็อกตรวจสิทธิ์
- ทุก path ที่ประกอบจากส่วนของ URL ต้องผ่าน `store.valid_id()` และ body ต้องมีเพดานขนาด
- **ข้อมูลที่ worker ส่งกลับเชื่อไม่ได้** ทุกฟิลด์ผ่าน `web/sanitize.py` และของที่เซิร์ฟเวอร์
  เป็นคนเลือกไว้แล้ว (ภาษาปลายทางของงานแปล/งานสรุป) ต้องอ่านจาก **spec เท่านั้น** ห้ามอ่านจาก
  ผลลัพธ์ (BUG-048 · BACKLOG #9b)
- ข้อความ error ที่ขึ้นเว็บเป็นของ **ผู้ใช้** ไม่ใช่ของคนดูแลเครื่อง — ห้ามมี path ของเครื่อง
  worker, log ดิบ, หรือ traceback ของ Python รายละเอียดออกทาง log ของ worker พร้อมรหัสอ้างอิง
  (BACKLOG #40 · #51/#52)
- ห้ามแก้ `.env` · ห้ามพิมพ์ค่าความลับ (ชื่อตัวแปรได้ ค่าห้าม) · ห้ามใส่ความลับในไฟล์ที่ track
  · ห้ามแตะ `bot/profile/` (มี session ที่ล็อกอิน Google ของเจ้าของ)
- **ห้ามทดสอบกับ production** (`meeting-ai-swart.vercel.app` และฐานข้อมูลจริง) และ
  **ห้ามรันเซิร์ฟเวอร์ทดสอบจาก `C:/project/meeting_ai`** เพราะ `config.root` ถูกตรึงไว้
  มันจะเขียนทับคลังการประชุมจริงของเจ้าของใน `recordings/web/`

## 3. สไตล์

- คอมเมนต์และข้อความถึงผู้ใช้เป็น**ภาษาไทย** ตัวระบุ/พาธ/คำสั่ง/รายละเอียด HTTP เป็นอังกฤษ
- **คอมเมนต์อธิบาย "ทำไม" ไม่ใช่ "ทำอะไร"** และมักอ้างเหตุการณ์ที่ทำให้ต้องเขียนแบบนั้น
  เขียนให้เข้ากับของเดิม — โค้ดที่ดูแปลกมักมีเหตุผลอยู่ ถ้าจะแก้ให้อ่านคอมเมนต์ก่อน
- `print()` มีได้เฉพาะ `cli.py` กับ `pipeline.py` (ข้อความที่ผู้ใช้อ่าน) ที่เหลือใช้
  `from . import log as _log` แล้ว `log = _log.get(__name__)` — ตั้งค่า logging ที่
  `cli.main` ที่เดียว ไลบรารีห้ามเรียก `basicConfig` (BACKLOG #35)
- ทุก `subprocess.run` ต้องมี `timeout` และคำสั่ง docker ไปทาง `bot._run()` / `bot._stop()`
  ซึ่งคิดเพดานเวลาให้ยาวกว่าระยะผ่อนผันเสมอ (BACKLOG #39)
- ข้อความ commit เป็นอังกฤษขึ้นต้นด้วย `feat:` / `fix:` / `chore:` ส่วนเนื้อความอธิบายเป็นไทยได้

## 4. วิธีทำงานที่โปรเจกต์นี้คาดหวัง

- **วัดก่อนเชื่อ** ตั๋วหลายใบในโปรเจกต์นี้ระบุอาการไว้ผิดหรือน้อยกว่าความจริง — วัดของจริงก่อน
  แล้วเขียนตัวเลขที่วัดได้ลงในตั๋ว/คอมมิต ("3.78 GB" ดีกว่า "กินแรมเยอะ")
- **เทสต์ต้องพิสูจน์ว่าไม่ vacuous** ย้อนกลไกที่เพิ่งแก้ออก แล้วยืนยันว่าเทสต์ล้มจริง
  เขียนจำนวนที่ล้มไว้ในคอมมิต ถ้าย้อนแล้วยังเขียวอยู่ แปลว่าเทสต์ไม่ได้ทดสอบอะไร
- **เทสต์ของที่ shipped ไม่ใช่สำเนา** ตรรกะในไฟล์เชลล์ให้ตัดฟังก์ชันออกมาจากไฟล์จริงแล้วรัน
- งานที่แตะหน้าเว็บ ให้เปิดเบราว์เซอร์ดูจริง และ `elementFromPoint` กลางปุ่ม — "วาดถูกที่"
  ไม่เท่ากับ "กดได้"
- อะไรที่ยืนยันไม่ได้ ให้บอกตรง ๆ ว่ายืนยันไม่ได้ และบอกว่าต้องทำอะไรถึงจะยืนยันได้

## 5. คำสั่งที่ใช้จริง

```bash
python -m compileall -q meeting_ai api bot          # ตรวจไวยากรณ์
PYTHONIOENCODING=utf-8 python -m unittest discover -s tests   # ชุดเทสต์
./mai web --no-open                                  # เว็บโหมดไฟล์ (พอร์ต 8765 เป็นของเจ้าของ)
./mai --help                                         # รายการคำสั่งย่อย
```

คอนโซลของเครื่องเจ้าของเป็น **cp874** เขียนภาษาไทยไม่ได้ ให้รันผ่าน `./mai` / `.\mai.cmd`
(ตั้ง `PYTHONIOENCODING=utf-8` ให้แล้ว) หรือตั้งเอง ไม่งั้นเจอ `UnicodeEncodeError`

`docs/product/BACKLOG.md` คือรายการงานจริง แถวที่ขึ้นต้นด้วย ✅ คือปิดแล้วพร้อมบันทึกว่าแก้อย่างไร
และ **เจอกับดักอะไรระหว่างทาง** — อ่านแถวที่เกี่ยวข้องก่อนเริ่มงานใหม่ในบริเวณเดียวกัน

## 6. สิทธิ์

ที่เก็บนี้**สงวนลิขสิทธิ์ทั้งหมด** (ดู [LICENSE](LICENSE)) เปิดสาธารณะไว้เพื่อความโปร่งใส
ไม่ได้อนุญาตให้ใครนำไปใช้ อย่าเพิ่มไฟล์สัญญาอนุญาตแบบโอเพนซอร์ส หรือข้อความที่สื่อว่าใช้ได้ฟรี
