# meeting_ai backlog

Ordered by what to do next · owner = agent role · source = QA report / ticket / PRD

Seeded 2026-09-16 from the full code review (see `docs/PROJECT-CONTEXT.md` → "Known state / hygiene red flags" for file:line). Re-verify each item before working on it; the code may have moved.

## P0 — users blocked, money or data at risk
| # | Item | Owner | Notes |
|---|---|---|---|
| 1 | ✅ Draft routes `tracks/upload-url`, `tracks/{name}`, `process` skip the ownership check — any logged-in user (or share-cookie holder) can upload into and start someone else's draft | backend-dev | **fixed 2026-09-16, merged to `main` (c0b1115, PR #1)** · `_draft_spec` + `_may_write_draft` · tests `test_p0_01_draft_ownership` |
| 2 | ✅ Share cookie passes the global auth gate → `GET /api/jobs`, `GET /api/workers` leak every user's active jobs and the worker roster | backend-dev | **fixed 2026-09-16, merged to `main` (c0b1115, PR #1)** · `_share_may_call` allow-list, `_workers_view` · tests `test_p0_02_share_gate` |
| 3 | ✅ `jobs.active()` / `store.job_active()` not scoped per user → other tenants' meeting titles inside `GET /api/meetings` | backend-dev | **fixed 2026-09-16, merged to `main` (c0b1115, PR #1)** · `_job_scope` → `jobs.active(owner_id, meeting_id)` · tests `test_p0_03_jobs_scoped` (team visibility → #37) |
| 4 | ✅ Concurrent bots share `recordings/bot/` as `/out`: `bot_status.txt` and screenshots collide, `_keep_debug_shot` deletes another job's live shots | backend-dev | **fixed 2026-09-16, merged to `main` (c0b1115, PR #1)** · `bot._job_slot`, `_stage_removable`, `_prune_stages(live=)` · tests `test_p0_04_bot_staging_per_job` |
| 5 | ✅ CLI (`mai process`, `record --process`, `bot`, `transcribe`) ignores `STT_PROVIDER` and always calls whisper.cpp | backend-dev | **fixed 2026-09-16, merged to `main` (c0b1115, PR #1)** · `stt.transcribe` in `pipeline`/`cli`, `--stt` flag · tests `test_p0_05_cli_uses_stt_provider` |
| 44 | ✅ **Arbitrary file write**: unvalidated translate `lang` → poisoned job id → worker audio endpoint wrote outside the data dir | backend-dev + security-engineer | **fixed (uncommitted) 2026-09-16** · `jobs.safe_job_id` + guard in `_worker_api()` and `claim()`, `MAX_ATTEMPTS=5`, `lang` allow-list · tests `test_bug_044_translate_lang_worker_path_write` (20 tests) · ticket [BUG-044](../tickets/BUG-044-translate-lang-worker-path-write.md) |
| 6 | ✅ Regression tests for #1-#5 (there are no tests at all today) | test-engineer | **done 2026-09-16** · `tests/_harness.py` + 5 modules, 64 tests · run `PYTHONIOENCODING=utf-8 python -m unittest discover -s tests -v` |

## P1 — wrong behaviour
| # | Item | Owner | Notes |
|---|---|---|---|
| 7 | ✅ `summarizer._chat` hard-caps `max_tokens: 4000` and ignores `finish_reason` → long summaries silently truncated (introduced in commit f068e8c) | backend-dev | **fixed 2026-09-16** · `LLM_MAX_TOKENS` / `LLM_MAX_TOKENS_CEILING`, `_stream_chat` returns `finish_reason`, `_chat` doubles the budget on `length` then raises at the ceiling · tests `test_backlog_07_summary_max_tokens` (14 tests) |
| 8 | ✅ No transcript chunking / map-reduce → multi-hour meetings overflow the LLM context | architect → backend-dev | **fixed 2026-09-18** · ADR: `docs/adr/ADR-001-transcript-chunking.md` (ตั๋วสั่ง ADR ก่อน) · **ขนาดของจริง** อนุมานจากประชุมใหญ่สุดใน production (149 นาที / 2,905 segment / payload 0.64 MB) ≈ 900 ตัวอักษร/นาที → 2 ชม. ≈ 108,000 ตัว = 54k–108k โทเคน เกิน context 32k แน่ · **อาการเดิมคือล้มถาวร** ไม่ใช่แค่ล้ม: 400 ไม่อยู่ใน `_RETRY_CODES` และปุ่ม "สรุปใหม่" ส่ง prompt ก้อนเดิมเป๊ะ ๆ กดกี่รอบก็ได้ผลเดิม · แก้ด้วย map-reduce แบบมีเงื่อนไข: เกิน `LLM_CHUNK_CHARS` (24,000) → แบ่งที่ขอบ segment → สกัด "ข้อเท็จจริง" ต่อก้อน → รวมด้วย `USER_TEMPLATE` ตัวเดิม (เทมเพลต/`target_lang`/กติกาผู้พูดจึงไม่ต้องดูแลสองทาง) · นับ **ตัวอักษร** ไม่ใช่โทเคน เพราะ tokenizer เป็น dependency หนักในโปรเจกต์ stdlib และก็ยัง ผิดอยู่ดีเมื่อ `.env` เปลี่ยนโมเดล · ความไม่แน่นอนนั้นรับด้วยการ **ถอยอัตโนมัติ**: ยิงตรงแล้วได้ 4xx (ไม่ใช่ 401/403/429) → ลอง map-reduce หนึ่งครั้ง โดยจงใจไม่จับคำใน error เพราะข้อความ ต่างกันตามผู้ให้บริการ · **บั๊กที่เจอระหว่างเขียนเทสต์**: ทางถอยแบ่งด้วยงบตัวเดิม ซึ่งเป็นงบที่ เพิ่งพิสูจน์ว่าใหญ่เกิน จึงได้ก้อนเดียวเสมอแล้ว raise ทิ้ง = โค้ดที่ไม่มีวันทำงาน ตอนนี้แบ่งด้วย งบที่เล็กลง · ประชุมสั้นไม่เปลี่ยนอะไรเลย (prompt เดิม จำนวนการเรียกเดิม) มีเทสต์คุมไว้ · tests `test_backlog_08_transcript_chunking` 22 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 6 · **ยังไม่ได้ยืนยันกับ LLM จริง** — ต้องใช้บทถอดเสียงจริง (ข้อมูลส่วนบุคคล) และเสียค่า API |
| 9 | ✅ `summarize()` always outputs Thai; `--lang` / meeting language never reaches the prompt | backend-dev | **fixed 2026-09-18** · `summarize(target_lang=...)` เดินจาก CLI `--summary-lang` → `pipeline.process_file(summary_lang=)` → `runner` อ่าน `spec["summary_lang"]` → prompt จริง · **ค่าเริ่มต้นยังเป็นไทย ไม่ใช่ตามภาษาของเสียง** — ผู้ใช้ไทยที่ประชุมภาษาอังกฤษส่วนใหญ่อยากได้สรุปไทย การเปลี่ยนค่าเริ่มต้นเป็นการตัดสินใจของเจ้าของ ผลิตภัณฑ์ ไม่ใช่ผลพลอยได้ของบั๊กฟิกซ์ (เทสต์ล็อก prompt ไทยไว้ทุกตัวอักษร) · `--lang` ยังหมายถึงภาษาของ **เสียง** เหมือนเดิม เป็นคนละแกนกับภาษาของ **สรุป** · ไม่ทำเทมเพลตแยกรายภาษา (5×5 = 25 ชุดที่ต้องตามแก้พร้อมกันตลอดไป) ใช้โครงไทยเป็นสเปก โครงสร้างแล้วสั่งให้แปลหัวข้อ วิธีเดียวกับ TRANSLATE_PROMPT · รหัสภาษาที่ไม่รู้จักส่งต่อให้ LLM ตามตัว ไม่เงียบ ๆ กลับไปเป็นไทย (นั่นคือบั๊กเดิมในรูปใหม่) · tests `test_backlog_09_summary_language` 17 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 10/17 · **ยังเหลือ**: หน้าเว็บยังสรุปเป็นไทยอย่างเดียว ปุ่ม "สรุปใหม่ด้วย AI" ยังไม่มีตัวเลือกภาษา (`build_spec` ส่ง `summary_lang` ต่อให้แล้ว เหลือ API + UI) ดูแถว #9b |
| 9b | ✅ Web: "สรุปใหม่ด้วย AI" has no language choice — `POST /resummarize` should accept `lang` and the summary in another language needs a home | backend-dev + web-dev | **fixed 2026-09-18** · `POST /resummarize` รับ `lang` (allow-list เดียวกับ `_translate`) ไม่ส่งมา/ส่ง `th` = ทางเดิมทุกประการ · สรุปภาษาอื่นได้ job id ของตัวเอง `<mid>.sum.<lang>` เพราะงานสรุปไทยใช้ job id = id การประชุม สั่งสองภาษาพร้อมกันจะทับกันเอง · ผลไปอยู่ `translations[lang]` ช่องเดียวกับคำแปล ผู้ใช้เลือกดูจาก `#d-lang` ตัวเดิม **สรุปต้นฉบับห้ามถูกทับ** และต่างจากปุ่ม "แปล" ตรงที่อันนี้สรุปตรงจากบทถอดเสียง ไม่ใช่แปลจาก สรุปไทย (บีบอัดสองชั้น) · **ภาษาปลายทางอ่านจาก spec เท่านั้น ห้ามอ่านจากผลที่ worker ส่งกลับ** (BUG-048 ซ้ำรอย) มีเทสต์ยิงผลปลอมที่มี `lang` ปนมาเพื่อยืนยัน · ผลที่ว่างเปล่าเป็น error ไม่ใช่คำแปลเปล่า ๆ และไม่เขียน `summary_error` ของการประชุม เพราะนั่นเป็นช่องของสรุปต้นฉบับ · ฝั่งหน้าเว็บ: ปุ่มเปลี่ยนข้อความตามภาษาที่เลือก ("สรุปใหม่เป็นภาษาอังกฤษ"), ภาษาที่ดูอยู่ไม่หายตอนหน้าโหลดซ้ำหลังงานเสร็จ, และป้าย "กำลังสรุปด้วย AI" รู้จัก job id รูปแบบใหม่ · tests `test_backlog_09b_resummarize_language` 19 เคส |
| 12 | ✅ Invite redemption TOCTOU: user created before `redeem_invite`, boolean result discarded | backend-dev | **fixed (uncommitted) 2026-09-17** · `claim_invite` / `claim_first_admin` decide in one statement before the user exists · review also closed an email-enumeration oracle the fix itself introduced · **proven on real PostgreSQL 16.15**: 12 concurrent signups on one invite gave 12 accounts before and 1 after; 12 concurrent first-signups gave 12 admins before and 1 after · tests `test_bug_012_invite_toctou` (12) · ticket [BUG-012](../tickets/BUG-012-invite-redemption-toctou.md) · no `db-init` needed |
| 10 | ✅ No rate limit on `POST /api/auth/login` (scrypt N=2¹⁴ also a CPU lever) | backend-dev + security-engineer | **fixed (uncommitted) 2026-09-17** · two buckets (10/15min cleared on success, 60/hour never cleared), rejected before scrypt, Postgres-backed so it works on serverless · review round closed 5 bypasses that made the first version ineffective · tests `test_bug_010_login_rate_limit*` (17) · ticket [BUG-010](../tickets/BUG-010-login-rate-limit.md) · **owner must run `./mai db-init`** |
| 11 | ✅ `_body_json` read an unbounded `Content-Length`; `_clean_segments` capped neither count nor text length | backend-dev | **fixed (uncommitted) 2026-09-16** · 64 KB control-plane / 8 MB transcript routes, 413, strict `Content-Length` · review round also closed keep-alive request smuggling, slowloris and a `NaN` that bricked a meeting permanently · tests `test_bug_011_body_size_caps` (43) · ticket [BUG-011](../tickets/BUG-011-unbounded-request-body.md) |
| 50 | ✅ Worker results over `MAX_JSON_TRANSCRIPT` (8 MB) come back 413, and the transcript is discarded with no retry | backend-dev | **fixed 2026-09-18** · **วัดของจริงก่อน: ประชุมใหญ่สุดในฐาน production (149 นาที 2,905 segment) ส่งจริง ~0.64 MB ห่างเพดาน 12 เท่า** ต้องยาว ~31 ชม. ถึงจะชน 413 — ของจริงที่เกิดง่ายกว่ามากคือเน็ตสะดุด/502/หมดเวลา ซึ่งของเดิมก็ทิ้งงานเหมือนกันเพราะยิงครั้งเดียว · `post_result()` ลองใหม่เฉพาะที่ลองแล้วมีหวัง (ต่อไม่ติด/408/429/5xx) ถอยแบบทวีคูณ 4→32 วิ · 4xx ไม่ลองซ้ำ · ส่งไม่ได้จริง ๆ ก็เก็บลงดิสก์ที่ `recordings/pending-results/` แล้ว `flush_pending()` ส่งใหม่ตอน worker เริ่มรอบหน้า · `ensure_ascii=False` ทำให้ payload ภาษาไทยเล็กลงเท่าตัว (413 ยิ่งไกลออกไปอีก) · ข้อความที่ส่งกลับไม่มี path ของเครื่อง worker (ระวัง BACKLOG #40) · tests `test_backlog_50_result_not_lost` 16 เคส |
| 51 | ✅ `except Exception: self._error(500, str(e))` in `_route()` returns raw Python exception text to the client (e.g. `invalid literal for int() with base 10: 'abc'`) | backend-dev + security-engineer | **fixed 2026-09-18** · `_oops()` เก็บ traceback ไว้ที่เซิร์ฟเวอร์พร้อมรหัสอ้างอิง 6 หลัก ส่งออกไปแค่ข้อความไทยกลาง ๆ + รหัส · ปิดรูเดียวกันอีก 3 จุดที่พบระหว่างไล่: `ต่อฐานข้อมูลไม่ได้: {e}` สองที่ (ข้อความ psycopg มีโฮสต์/พอร์ต/ชื่อฐานจริง) และ `transcribe_clip` (บอก endpoint ของ STT) · **ไม่แตะข้อความไทยที่เราเขียนเองให้ผู้ใช้อ่าน** (BadBody, 404, RuntimeError จาก apply_result) มีเทสต์คุมทั้งสองด้าน · query string ไม่ถูกเขียนลง log · tests `test_backlog_51_52_error_hygiene` |
| 52 | ✅ Every response carries `Server: meeting_ai Python/3.12.10`, disclosing the interpreter version | devops-engineer | **fixed 2026-09-18** · override `version_string()` คืนแค่ `server_version` — ตัดทั้งหัวข้อไม่ได้เพราะ `http.server` เขียนเองทุกคำตอบ · ตรวจทั้งคำตอบ 200 และ 404 |
| 53 | ✅ **Structured action items** — ครบสามขั้น 2026-09-20 | architect → backend-dev → web-dev | [PRD](PRD-structured-action-items.md) · โมดูลใหม่ `web/actionitems.py` (`parse` / `normalize` / `reconcile`) + คอลัมน์ `action_items jsonb` + `set_action_item()` / `delete_action_item()` **ทั้งสองแบ็กเอนด์** · ซิงก์อัตโนมัติที่ `create` / `set_summary` / `update` ครบสามทางที่เขียนสรุป (ทางที่สี่คือ `set_translation` ซึ่ง **ไม่** ซิงก์ ตาม PRD 6.1) · **ข้อ ❓ ของเจ้าของ เดินตามข้อเสนอใน PRD ทุกข้อ** (6.1 แยกจาก `summary` ภาษาต้นฉบับ · 6.2 ผู้รับผิดชอบเป็นข้อความอิสระ · 6.5 เก็บเป็น JSON บนแถวการประชุม) **ยกเว้น 6.4 (เครื่องหมายถูกในไฟล์ส่งออก) ที่ยังไม่ทำ** เพราะเปลี่ยนหน้าตาไฟล์ที่คนเคยชิน และ 6.3 (สิทธิ์ลิงก์แชร์) จะมีผลจริงตอนมีเส้น API · **ต่างจาก PRD หนึ่งข้อ**: PRD ร่าง API เป็น `.../action-items/{index}` แต่ index ขยับทุกครั้งที่สรุปถูกเขียนใหม่ ถ้าผู้ใช้กดติ๊กพอดีกับที่งานสรุปใหม่เพิ่งจบจะติ๊กโดนคนละรายการ — ระเบียนจึงมี `id` ของตัวเอง (ไม่ฝังลง Markdown ซึ่งคือทาง D ที่ PRD ปฏิเสธ) · **คอลัมน์ใหม่ต้องไม่ทำ production พังก่อน migration** (บทเรียนจาก peaks): `_has_action_items()` แบบเดียวกับ `_has_peaks()` และมีเทสต์ที่จำลองฐานที่ยังไม่ได้รัน `db-init` แล้วยืนยันว่า**ไม่มี SQL สักคำสั่งที่เอ่ยถึงคอลัมน์นี้หลุดออกไป** — เทสต์เวอร์ชันแรกนับจำนวนคำแล้วมุตันต์รอดไปได้ จึงเปลี่ยนเป็นตรวจพฤติกรรม · tests `test_backlog_53_action_items_backend` (32) + `test_backlog_53_prd_action_items` (18) · กลับกลไก 11 แบบแดงทุกแบบ · **เหลือ**: เส้น API, หน้าเว็บ (เช็กลิสต์ + กลุ่ม “ไม่อยู่ในสรุปล่าสุด”), e2e · **ขั้นที่ 2 (2026-09-20)**: `PATCH|DELETE /api/meetings/{id}/action-items/{item_id}` วาง **หลัง** บล็อกตรวจสิทธิ์ของ `_meeting()` ตามกติกาใน CLAUDE.md จึงได้ `_may_write()` ตัวเดียวกับการเกลาสรุปมาฟรี ๆ — นั่นคือคำตอบของ PRD 6.3 (ลิงก์แชร์แบบแก้ได้ ติ๊กได้) ถ้าจะให้เข้มกว่านั้นต้องแยกเงื่อนไขที่บล็อกนั้น ไม่ใช่ที่ handler · `_ITEM_ID_RE` ตรวจรูปแบบรหัสก่อนเอาไปค้น (มาจาก URL เหมือน mid) · **ต้องเติม `set_action_item`/`delete_action_item` ให้ `FakeStore` ด้วย** ไม่งั้นเส้น cloud ไม่เคยถูกทดสอบเลยและจะตายด้วย AttributeError ตอนรันจริง (บทเรียนเดียวกับตอน `rate_hit`/`rate_reset` หายไป) · tests `test_backlog_53_action_items_api` (20) · กลับกลไก 8 แบบแดงทุกแบบ · **เทสต์ตำแหน่งเส้นเวอร์ชันแรกอ่อน**: ยึดข้อความ “ไม่มีสิทธิ์กับการประชุมนี้” ซึ่งโผล่ในบล็อก draft ที่อยู่ก่อนหน้าด้วย ย้ายเส้นไปผิดที่จริง ๆ แล้วมันยังเขียว (จับได้จากเทสต์พฤติกรรมแทน) แก้ให้ยึดบรรทัดที่ตัดสินสิทธิ์จริง · **ขั้นที่ 3 (2026-09-20)**: เช็กลิสต์เป็น **แท็บที่สาม** ของหน้ารายละเอียด (`สรุป | สิ่งที่ต้องทำ | บทถอดเสียง`) ต่อยอดจาก #80 ที่เพิ่งทำให้แท็บใช้ได้ทั้งสองจอ — ตรงกับ US-1 “เห็นรายการโดยไม่ต้องอ่านสรุปทั้งฉบับ” · **สรุปที่ไม่มีตาราง = ซ่อนแท็บไปเลย** ไม่ใช่กดแล้วเจอหน้าว่าง (และต้องกรองปุ่มที่ซ่อนออกจาก `btns` ไม่งั้น `pick()` เลือกแท็บที่มองไม่เห็นได้) · กลุ่ม “ทำแล้ว แต่ไม่อยู่ในสรุปล่าสุด” มีปุ่มลบเฉพาะแถว orphan · ติ๊กพลาดแล้ว **คืนสถานะช่องติ๊กให้ตรงกับเซิร์ฟเวอร์** ไม่ปล่อยให้หน้าจอโกหก · วัดในเบราว์เซอร์จริง 375x812: กล่องติ๊ก 20x20 hit-test โดนทุกแถว · กดติ๊กจริงแล้ว**ยิงถึงเซิร์ฟเวอร์จริง** (`done=true`) · กดลบ orphan จริงแล้วเหลือ 2 แถวทั้งบนหน้าจอและที่เซิร์ฟเวอร์ · tests `test_backlog_53_action_items_ui` (20) · กลับกลไก 12 แบบแดงทุกแบบ (รวม “ไม่ escape ข้อความงาน” = ช่องโหว่ XSS และ “อ้างด้วยลำดับแทน id”) |
| 54 | 🚧 **Ad-hoc Q&A** — ADR-002 ยอมรับแล้ว · **ฝั่งหลังบ้านทำแล้ว 2026-09-20 · ยังไม่มีหน้าเว็บ** | architect → backend-dev → web-dev | [ADR-002](../adr/ADR-002-ad-hoc-qa.md) · `summarizer.answer()` (ชั้นที่ 1 ตอบจาก**สรุป**อย่างเดียว) + งานชนิดใหม่ `ask` ใน `runner` + `jobs.submit_ask()` id `<mid>.ask.<hex6>` + คอลัมน์ `qa jsonb` + `add_qa()`/`delete_qa()` ทั้งสองแบ็กเอนด์ + `POST /api/meetings/{id}/ask` และ `DELETE .../qa/{id}` · **ข้อ ❓ ทั้งห้าตัดสินแล้ว** (บันทึกใน ADR §5.0): 5.1 ชั้นเดียว + เก็บธง `enough` ไว้เก็บตัวเลขจริงว่าไม่พอบ่อยแค่ไหน · 5.2 ไม่ให้เว็บเรียก LLM เอง · 5.4 เก็บคำตอบ 20 คู่ล่าสุด · 5.5 ไม่อ้างเวลา (สรุปไม่มีเวลาให้อ้าง อ้างไปก็คือแต่ง) · **5.3 เลือกต่างจากที่ ADR เอนไป: ต้องมีสิทธิ์แก้ถึงจะถามได้** เพราะการถามใช้เงินของเจ้าของ และลิงก์แชร์แบบอ่านอย่างเดียวมักส่งให้คนนอก ปลดล็อกทีหลังง่ายกว่าตามเก็บบิล · **เพดานผูกกับคน ไม่ใช่ IP** (`ask:<uid>` → `ask.share:<sha256 ของ token>` → `ask.ip:`) 20 ครั้ง/10 นาที · **ผมเขียนเงื่อนไขเพดานกลับด้านตอนแรก** (`_bucket_hit` คืนวินาทีที่ต้องรอ ไม่ใช่ True/False) เพดานเลยไม่ทำงานเลย — จับได้ตอนอ่านโค้ดเดิม ไม่ใช่ตอนเทสต์ · tests `test_backlog_54_ask_backend` (42) · กลับกลไก 17 แบบ · **รอบแรกรอด 3 แบบ**: “ถอดด่านคำถามว่างในงาน” (ไปตายที่ summarizer แทน เทสต์เลยไม่รู้) · “โควตาผูกกับ IP” (เทสต์ดูแค่ลำดับบรรทัดในซอร์ส) · “FakeStore ไม่มี add_qa” (ไม่มีเทสต์ไหนเดินถึง) แก้ทั้งสามแล้วรอบสองแดงครบ · **เหลือ**: หน้าเว็บ และชั้นที่ 2 (ค้นบทถอดเสียงคำต่อคำ) ซึ่งรอตัวเลขจาก `enough` ก่อนตามข้อ 5.1 |
| 46 | ✅ `GET /api/meetings/{id}/audio` with `Range` past EOF returns 200 + whole file instead of 416 + `Content-Range: bytes */<size>` | backend-dev | **fixed 2026-09-18** · `_parse_range()` เดิมยุบสองกรณีที่ RFC 9110 แยกไว้เข้าด้วยกันแล้วคืน `None` ทั้งคู่ — "อ่านไม่ออก" (ต้องเมินแล้วเสิร์ฟทั้งไฟล์ 200) กับ "สนองไม่ได้" (ต้อง 416) ตอนนี้คืนสามแบบ · ผลของบั๊กเดิม: ผู้เล่นที่เลื่อนไปท้ายคลิปได้ไฟล์ทั้งก้อนไหลผ่าน serverless function และยังไม่รู้อยู่ดีว่าขอเกินไปแล้ว · เจอระหว่างทาง: `bytes=5-2` ผ่าน regex เข้าไปได้ แล้ว `length = end - start + 1` ติดลบ → ส่ง `Content-Length: -2` ออกสาย (ไคลเอนต์พังจริง เห็นตอนย้อนกลไกทดสอบ) · `bytes=-0` ก็เข้าข่ายสนองไม่ได้ ไม่ใช่ "ทั้งไฟล์" · tests `test_backlog_46_range_not_satisfiable` 17 เคส ยิง HTTP จริง รวมเส้นแบ่ง size-1 = 206 / size = 416 · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 6/17 |
| 47 | ✅ Tab-share option is not hidden at mobile width — feature-detection only, and sets `disabled` not `hidden` | web-dev | **fixed 2026-09-18** · ส่วน hidden + เช็คความกว้าง ลงไปพร้อมรอบ mobile redesign (`3b4710a`) ที่เหลือคือ `updateTabModeAvailability()` ถูกเรียกครั้งเดียวตอน `setupSources()` — ความกว้างเปลี่ยนระหว่างใช้งานได้จริง (หมุนแท็บเล็ต ย่อหน้าต่าง สลับโหมดอุปกรณ์ใน DevTools ซึ่งเป็นกรณีที่ตั๋วยกมาเอง) · ตอนนี้ `TAB_MODE_Q` เป็น MediaQueryList ตัวเดียวที่ฟัง `change` และตอนสลับกลับเป็น room จะจำค่าใหม่ + วาดแผงแหล่งเสียงใหม่ (ไม่จำ = ครั้งหน้าโหลดโหมดที่ซ่อนอยู่กลับมาอีก) · **ยืนยันในเบราว์เซอร์จริง** (Chromium, โคลน `tpl-new` เข้า DOM): 1024px → `display: flex` กดได้ `elementFromPoint` ตกในป้ายนั้นจริง เลือก "tab" ได้ · ย่อเป็น 390px → `change` ยิง, `hidden`, `display: none`, rect 0×0, `disabled`, สลับกลับเป็น room เอง · breakpoint 560px ตรงกับ `@media (max-width: 560px)` ใน style.css และมีเทสต์ผูกสองค่านี้ไว้ · tests `test_backlog_47_tab_mode_mobile` 12 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 5/12 |
| 49 | ✅ Audit production for pre-patch poisoned job ids before deploying the BUG-044 fix | owner + devops-engineer | **done 2026-09-17** · `python scripts/audit_job_ids.py` against production: **40 jobs, 0 that the guard would reject** — the arbitrary-file-write chain was never exploited, and no data cleanup was needed before deploying |
| 13 | ✅ **fixed via #44** — Translate `lang` unvalidated → enters job id `<mid>.tr.<lang>` and the LLM prompt (prompt injection confirmed; junk key `../../pwn` stored in `translations`) | backend-dev | restrict to `LANGUAGE_NAMES`. Fix together with #14 |
| 14 | ✅ **fixed via #44** — Worker audio endpoint builds `WEB_DIR / f"{job_id}.{ext}"` without `valid_id`; `unquote(job_id)` at `server.py:877` happens after the path split, so `%2F..%2F` survives | backend-dev | `server.py:902-905`. Fix together with #13 |
| 15 | ✅ Static file guard uses `str.startswith` instead of `Path.is_relative_to` | backend-dev | **fixed 2026-09-17** · `_static()` เทียบด้วย `Path.is_relative_to(root)` · PoC จริง: `GET /static/../static_backup/secret.txt` เคยคืน 200 พร้อมเนื้อไฟล์ ตอนนี้ 404 · เป็นการปิดคลาสของบั๊ก (prefix confusion) ไม่ใช่ช่องที่ยิงได้บนซอร์สทรีปัจจุบัน — ข้าง `static/` ยังไม่มีโฟลเดอร์ชื่อขึ้นต้นเหมือนกัน · ticket [BUG-015](../tickets/BUG-015-static-path-prefix-guard.md) |
| 16 | ✅ `GET /s/<token>` sets `mai_share` on plain navigation → share-cookie fixation + SPA auto-navigate | backend-dev + web-dev | **fixed 2026-09-17** · GET เสิร์ฟหน้ายืนยันเฉย ๆ ไม่ตั้งคุกกี้ · ย้ายการตั้งคุกกี้ไป `POST /api/auth/share` ที่บังคับ `Content-Type: application/json` (ฟอร์มข้ามเว็บส่งไม่ได้ และ fetch ข้ามโดเมนต้อง preflight ซึ่งเซิร์ฟเวอร์ไม่ตอบ CORS เลย) · `app.js` เช็คโทเคนใน URL ก่อน `needsAuth()` · ticket [BUG-016](../tickets/BUG-016-share-cookie-fixation.md) · เหลือ: ยังไม่มี "ออกจากโหมดแชร์" (`DELETE /api/auth/share` + ปุ่ม) และยังไม่มี rate limit บนการเดาโทเคน |
| 17 | ✅ API-path chunking uses `-c copy` but offsets `i * 900` → timestamp drift on long files | backend-dev | **fixed 2026-09-18** · **ของจริงหนักกว่าที่ตั๋วเขียน**: Ogg เก็บ granule position เป็นเวลาสัมบูรณ์ของสตรีมเดิม ตัดด้วย `-c copy` เฉย ๆ ท่อนที่ i จึงเป็นไฟล์ที่ **ประกาศว่าตัวเองยาว (i+1)×900** ทั้งที่มีเสียงอยู่ช่วงเดียว (วัดจริงด้วย ffprobe: ตัดท่อนละ 7 วิ ได้ duration 7, 14, 21, 28...) ฝั่ง API ก็ decode ด้วย libav เหมือนกัน มันคืน timestamp ที่บวก offset มาแล้ว โค้ดบวก `i*900` ทับอีก = **เลื่อนสองเท่า** ไม่ใช่คลาดนิดหน่อย · แก้ด้วย `-reset_timestamps 1` (ไทม์ไลน์ของแต่ละท่อนเริ่มที่ 0 ตามที่โค้ดสมมติไว้อยู่แล้ว) และถาม offset จาก `-segment_list csv` ที่ ffmpeg เขียนเอง แทนการคำนวณจากลำดับท่อน — `-segment_time` แปลว่า "ตัดที่แพ็กเก็ตแรกตั้งแต่เวลานี้ไป" ไม่ใช่ตัดตรงเป๊ะ · อ่านรายการไม่ได้ = ถอยไปใช้ค่าตามลำดับ พร้อมเตือน ไม่ปล่อยให้เพี้ยนเงียบ ๆ · tests `test_backlog_17_18_api_chunking` (ยิง ffmpeg จริง เพื่อพิสูจน์พฤติกรรม ไม่ใช่เชื่อคำอธิบาย) |
| 18 | ✅ `drop_hallucinations()` only on the local path; API transcripts keep repeated-phrase junk | backend-dev | **fixed 2026-09-18** · การวนคำเดิมซ้ำ ๆ ตอนเจอช่วงเงียบ เป็นพฤติกรรมของ **โมเดล** ไม่ใช่ของ **ที่รัน** ฝั่ง API จึงวนเหมือนกัน · เรียก `transcriber.drop_hallucinations()` ตัวเดียวกันทั้งสองทาง (เทสต์ล็อกไว้ว่าต้องเป็นฟังก์ชันเดียวกัน ไม่ใช่ตัวกรองสองชุดที่วันหนึ่งจะเพี้ยนกันเอง) · ทดสอบร่วมกับ #17 ใน `test_backlog_17_18_api_chunking` 15 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 7 เคส |
| 19 | ✅ `stt.resolve()` silently falls back local → API when the model is missing (privacy) | backend-dev | **fixed 2026-09-17** · `config.stt_provider_set` แยก "สั่ง local เอง" ออกจาก "ค่าเริ่มต้นเป็น local" → สั่งเองแล้วไม่มี whisper = RuntimeError ระบุ host ปลายทาง ไม่อัปโหลดให้ · ไม่ตั้งอะไรเลย (Vercel) ยังไป api ได้แต่พิมพ์เตือนระบุ host+โมเดล · ticket [BUG-019](../tickets/BUG-019-silent-stt-fallback.md) · เหลือฝั่ง UI: หน้าเว็บยังไม่แสดงว่าประชุมที่เสร็จแล้วถอดด้วยอะไร (`result["stt"]`) → web-dev · regression tests ตาม acceptance criteria → test-engineer |
| 20 | ✅ `worker --once`: heartbeat stops before the in-flight job finishes → server reaps the job | backend-dev | **fixed 2026-09-17** · `beat()` เต้นจนกว่าจะ "ถูกสั่งหยุด **และ** ไม่มีงานค้าง" · drain เปลี่ยนจาก `for _ in range(600)` เป็น deadline `DRAIN_MAX_SEC = 6h` พร้อมพิมพ์ id งานที่ถูกทิ้ง · **กลไกจริงต่างจากที่แถวนี้เคยเขียน**: `jobs_reap()` ดู `jobs.updated_at` ไม่ใช่ heartbeat และถูกเรียกจาก `jobs.claim()` ที่เดียว · ต้องอัปเดตโค้ดบน**เครื่อง worker** อัปเดต Vercel อย่างเดียวไม่ช่วย · ticket [BUG-020](../tickets/BUG-020-worker-once-heartbeat-gap.md) |
| 21 | ✅ Bot container runs as root with `--no-sandbox`, no `HEALTHCHECK`; `x11vnc -nopw`; Zoom passcode + URL via docker env | devops-engineer + security-engineer | **fixed 2026-09-18** · บอทคือพื้นผิวโจมตีที่ใหญ่ที่สุดในระบบ: เปิดหน้าเว็บของคนอื่นด้วย Chromium โดยมี session ที่ล็อกอิน Google ของเจ้าของ mount อยู่ที่ `/prof` · **ไม่รันเป็น root**: `USER pwuser` (uid 1000 มากับ image ของ Playwright) และ entrypoint ตรวจสิทธิ์เขียน `/out`/`/prof` ก่อนทำอะไรทั้งสิ้น — ล้มพร้อมบอกวิธีแก้ ดีกว่าอาการ "บอทนั่งจนจบแล้วไม่มีไฟล์เสียง" ซึ่งไล่ยากมาก · **`-nopw` หายไป**: รหัสสุ่มใหม่ทุกครั้งจากฝั่ง host ส่งเข้าไปทาง env แล้ว entrypoint เขียนเป็น `rfbauth` และ `unset` ทิ้ง ไม่มีรหัส = ไม่เปิดจอเลย (จอนั้นคือเบราว์เซอร์ที่กำลังล็อกอินบัญชี Google ของเจ้าของ) · **passcode ไม่ไปทาง `-e`** อีกแล้ว: `docker inspect` แสดง env ให้ทุกคน ในกลุ่ม docker เห็นและค้างตลอดอายุ container — ส่งเป็นไฟล์ 0600 ในโฟลเดอร์พัก อ่านแล้วลบทันที ทั้งฝั่ง container และฝั่ง host · เพิ่ม `HEALTHCHECK` เฝ้า Xvfb · **`--no-sandbox` ยังอยู่** — ถอดออกต้องมี seccomp/`SYS_ADMIN` ซึ่งทดสอบไม่ได้จากที่นี่ ดูแถว #21b · tests `test_backlog_21_22_supply_chain` · **ยังไม่ได้รัน container จริง** (เครื่องนี้ไม่มี Docker daemon) เจ้าของต้องรัน `./mai bot` หนึ่งรอบ |
| 21b | `--no-sandbox` ยังอยู่ในทั้ง join_meeting.py และ login.py — ถอดออกต้องให้ container มี seccomp profile ของ Chromium หรือ `--cap-add=SYS_ADMIN` ซึ่งเปลี่ยนรูปคำสั่ง `docker run` และต้องทดสอบกับห้องประชุมจริง | devops-engineer + security-engineer | **กลไกพร้อมแล้ว 2026-09-18 · ค่าเริ่มต้นยังปิดโดยตั้งใจ — รอเจ้าของยืนยันกับห้องจริง** · runbook: [enable-bot-sandbox.md](../runbooks/enable-bot-sandbox.md) · `--no-sandbox` ไม่ได้ฝังตายตัวอีกแล้ว ย้ายไป `bot/platforms.py` → `sandbox_args()` ซึ่งดู env `CHROMIUM_SANDBOX` ที่ฝั่ง host ตั้งมาให้ · ฝั่ง host `bot._sandbox_flags()` ตั้ง **ทั้งธงของ docker และ env พร้อมกันที่เดียว** เพราะสองฝั่งไม่ตรงกันคือทางเดียวที่ ฟีเจอร์นี้พังเงียบ ๆ (Chromium ไม่เปิดเลย = บอทไม่เข้าห้อง = ประชุมหายทั้งครั้ง) · เปิดได้สองทาง: `MAI_BOT_SECCOMP=<ไฟล์>` (แคบกว่า ใช้แทน SYS_ADMIN ไม่ใช่เพิ่มจากมัน และตรวจว่าไฟล์มีอยู่จริงก่อนเรียก docker) หรือ `MAI_BOT_SANDBOX=1` (`--cap-add=SYS_ADMIN`) · ครอบทั้งคอนเทนเนอร์เข้าห้อง**และ**คอนเทนเนอร์ล็อกอิน ซึ่งเป็นตัวที่ผู้ใช้กรอกรหัส Google ลงไป · tests `test_backlog_21b_chromium_sandbox` (15) จับคำสั่ง `docker run` ที่ประกอบได้จริง โดยไม่ต้องมี Docker daemon · **ยังยืนยันไม่ได้: เครื่องนี้ไม่มี Docker daemon จึงไม่รู้ว่า Chromium เปิดขึ้นจริงไหมเมื่อเปิด sandbox** ต้องทำพร้อมรอบที่เจ้าของรัน `./mai bot` ยืนยัน #21 ตามที่ตั๋วเขียนไว้แต่แรก |
| 22 | ✅ Downloads (models, whisper binaries) unverified; `setup-ubuntu.sh` clones whisper.cpp without a tag | devops-engineer | **fixed 2026-09-18** · `models/SHA256SUMS` + `verify()` ในทั้งสองสคริปต์ติดตั้ง (macOS ใช้ `shasum -a 256` เพราะไม่มี `sha256sum` ติดมา) ไฟล์ที่ไม่ตรงถูก **ลบทิ้ง** ไม่ใช่ปล่อยไว้ ไม่งั้นรอบหน้าเจอแล้ว คิดว่า "มีอยู่แล้ว" · โมเดลที่ไม่มีในรายการ = เตือนว่าไม่ถูกตรวจ แต่ไม่บล็อกการติดตั้ง · **ที่มาของ sha256 ไม่ใช่ตัวเลขที่คิดเอง**: ตรงกันสองทาง — `sha256sum` ของไฟล์ที่เครื่องเจ้าของ ใช้อยู่จริง และฟิลด์ `lfs.sha256` ที่ Hugging Face ประกาศผ่าน API ซึ่งยืนยันด้วยว่าโมเดลในเครื่อง ไม่เคยถูกแก้ · whisper.cpp ปักหมุดที่ `v1.9.4` **และ commit sha** แล้วตรวจ `rev-parse HEAD` หลัง clone — tag ถูกย้ายไปชี้ commit อื่นได้ การ clone ตาม tag เฉย ๆ จึงไม่ใช่การปักหมุด · เทสต์ตัด `verify()` จากไฟล์จริงมารันด้วย bash กับไฟล์จริงสี่เคส |
| 37 | ✅ Jobs list scoped by `spec->>'owner_id'` only: jobs on a `team` meeting submitted by another member, and pre-deploy jobs without owner_id, are invisible to the meeting owner | backend-dev + web-dev | **fixed 2026-09-18** · `pgstore.job_active()` เพิ่มสาขา "งานบนการประชุมที่คนนี้อ่านได้" (`exists (select 1 from meetings m where m.id = j.meeting_id and (m.owner_id = ผู้ดู or m.visibility = 'team'))`) ใช้กติกาเดียวกับการเห็นตัวการประชุม **ไม่ได้เปิดกว้างกว่าเดิม** — ประชุมส่วนตัวของคนอื่นยังไม่เห็น · งานที่ไม่มีทั้ง owner_id และ meeting_id ยังเห็นได้เฉพาะแอดมิน · tests `test_backlog_37_team_job_visibility` 8 เคสยิง Postgres จริง · พิสูจน์ไม่ vacuous: ใช้ SQL เดิม → ล้ม 3 เคสที่เป็นของที่แก้ ส่วน 5 เคสที่คุมเส้นความปลอดภัยยังผ่าน |
| 38 | ✅ `worker_tag()` truncates to 16 alnum chars: `meeting-ai-worker-01`/`-02` collide → one worker's `cleanup_stale`/`_prune_stages` can hit the other's containers and staging dirs | backend-dev + devops-engineer | **fixed 2026-09-18** · `slug[:12] + md5(ชื่อเต็ม)[:6]` — ต่อ hash ไว้เสมอ ความไม่ซ้ำไม่ขึ้นกับจุดตัด slug · ความเสียหายจริงคือ `cleanup_stale()` **สั่ง docker stop** worker ที่เพิ่งเริ่มจึงหยุดบอทของอีกตัวที่กำลังอยู่ในห้องประชุมจริง (โฟลเดอร์พักมีด่าน live + wav ไม่ว่างช่วยไว้บางส่วน แต่ container ไม่มีอะไรกัน) · tests `test_backlog_38_worker_tag` 11 เคส รวมเคสที่ยืนยันว่าอัลกอริทึมเดิมชนจริง · พิสูจน์ไม่ vacuous: สลับกลับไปใช้ของเดิม → ล้ม 6/11 · **ตอนอัปเกรด**: container ที่สร้างด้วยโค้ดเก่าไม่เข้าขอบเขต cleanup ใหม่ ควรรีสตาร์ต worker ตอนไม่มีบอททำงาน |
| 39 | ✅ `bot.py` raw `subprocess.run` docker calls without timeout (`docker rm -f` in `join_and_record`, `docker stop` in `leave()`, `login()`), and `proc.wait(timeout=120)` raises uncaught `TimeoutExpired` instead of a Thai error | backend-dev | **fixed 2026-09-18** · ทุก `subprocess.run` ในไฟล์มี timeout แล้ว ผ่าน `_stop()`/`_rm()` และ `docker build` ได้เพดาน 45 นาที + ข้อความไทยแทน `CalledProcessError` · **กับดักที่เกือบพลาด**: `_run()` มีเพดาน 25s ถ้า route `stop -t 30` เข้าไปเฉยๆ ตามที่ตั๋วบอก เพดานจะมาถึงก่อนระยะผ่อนผันจะหมด = ตัดคำสั่งหยุดของตัวเองตอนบอทกำลังปิดไฟล์เสียง `_stop()` จึงคิด timeout = grace + 30 เสมอ · **ของหนักกว่าที่ตั๋วเขียน**: `proc.wait(timeout=120)` เปล่าๆ ทำให้ `TimeoutExpired` หลุดออกจาก `join_and_record` ทั้งดุ้น ข้ามท่อนย้ายไฟล์เสียง — เสียงทั้งชั่วโมงค้างในโฟลเดอร์พัก แล้วงานถูกรายงานว่าล้ม · `_wait_or_kill()` สั่ง `docker kill` ตัว container (ไม่ใช่แค่ client ที่ปล่อยให้ container เขียน /out ต่อขณะเราย้ายไฟล์) แล้วเดินต่อพร้อมคำเตือนในบันทึกงาน · tests `test_backlog_39_docker_timeouts` 17 เคส รวมตัวตรวจ AST ที่จับ `subprocess.run` ที่ลืม timeout ในอนาคต · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 7/17 |
| 42 | Verify production has no `meetings.owner_id is null` rows: `pgstore.access()` grants `owner` to every user for such rows (intended for data migrated from file mode) | owner / devops-engineer | **เครื่องมือพร้อมแล้ว 2026-09-18 · ตัวเลขจริงยังต้องให้เจ้าของรัน — agent ไม่แตะ production** · `./mai db-check` อ่านอย่างเดียว คืนแค่จำนวน ไม่คืนข้อมูลของใคร จึงรันกับฐานจริงได้ (exit 1 เมื่อข้อที่กระทบสิทธิ์ไม่ผ่าน เอาไปใส่สคริปต์ได้) · **ตั๋วสันนิษฐานผิดว่าแถวแบบนี้มีแต่ของเก่าที่ย้ายมาจากโหมดไฟล์** — schema เขียน `owner_id uuid references users(id) **on delete set null**` ดังนั้น **ลบผู้ใช้หนึ่งคนในฐาน = การประชุมทั้งหมดของเขากลายเป็นของทุกคนทันที** (เทสต์พิสูจน์ด้วยการลบผู้ใช้จริงแล้วดูสิทธิ์เปลี่ยน) · ไล่แล้วว่า **ไม่มีเส้น API ไหนสร้างแถวไม่มีเจ้าของได้** (`_create_draft` ใช้ `self.user_id` เสมอ และคนถือลิงก์แชร์ POST `/api/meetings` ไม่ได้) ทางเกิดจึงเหลือ สองทาง: ข้อมูลที่ย้ายมา กับการลบผู้ใช้ด้วยมือ · ผลของแถวแบบนี้วัดกับ Postgres จริงแล้ว: คนแปลกหน้าที่ล็อกอินได้สิทธิ์ `owner` (ลบ/แชร์/เปลี่ยน visibility) **และ**เห็นมันในรายการของตัวเอง · `access()` ยังให้สิทธิ์เหมือนเดิม (ไม่กล้าเปลี่ยนก่อนรู้ว่ามีแถวแบบนี้กี่แถว) แต่ตอนนี้ **เขียน log ระดับ warning ทุกครั้งที่ใช้ทางนั้น** จะได้ไม่เงียบ · tests `test_backlog_42_ownerless_meetings` (13 — 5 ตัวต้องมี Postgres จริง รันใน CI งาน ubuntu+postgres) · ⚠️ **เจ้าของต้องรัน `./mai db-check` เอง** ถ้า `ownerless_meetings` ไม่ใช่ 0 ให้เซ็ต `owner_id` ให้ถูกคนก่อน แล้วค่อยพิจารณาปิดทาง null ใน `access()` |

| 48 | ✅ `apply_result` trusted the worker's `lang`, and passed its `segments` straight into `store.create` with no validation at all | backend-dev | **fixed (uncommitted) 2026-09-17** · new `web/sanitize.py` holds the shared rules; `lang` now comes from the job spec · a worker could write `NaN` and make `GET` and every export return 500 permanently — the same bug BACKLOG #11 closed on the user path, through the other door · tests `test_bug_048_*` (52) · ticket [BUG-048](../tickets/BUG-048-worker-result-trusted-fields.md) |
| 56 | ✅ **Lost update in the file store across processes.** Read-modify-write guarded only by a `threading.RLock`, so `mai web` and a CLI run could each write the whole index back and destroy the other's record | backend-dev | **fixed (uncommitted) 2026-09-17** · cross-process lock (`msvcrt` / `fcntl`) around the whole read-modify-write · measured: Windows lost 152/500, **Linux lost up to 250/320 (78%)**, both 0 after · also fixed a Windows `os.replace` PermissionError that hit 83% of writes when a reader had the file open · tests `test_bug_056_*` (14) · ticket [BUG-056](../tickets/BUG-056-file-store-lost-update.md) |
| 57 | ✅ The 64-test suite never touched the file store's **write** path — a constant that does not exist made `store.create` raise on every call and the suite still passed 64/64 | test-engineer | **ยืนยันด้วยการวัด 2026-09-18 — ไม่ใช่เชื่อคำอ้างของตั๋วเอง** · เดิมแถวนี้เขียนว่า “closed as part of #56” ซึ่งเป็นการอ้างตัวเอง จึงเอา**บั๊กจริง**ใส่กลับ เข้าไปใน `store.py` (`msvcrt.LK_NBLCK` → `LOCK_NBLCK` ตัวเดิมเป๊ะ) แล้วนับ: **สวีททั้งชุดแดง 62 ตัว** (9 fail + 53 error) — ช่องว่างปิดจริง · ตัดไฟล์ `test_bug_056_file_store_basic.py` ที่ตั๋วให้เครดิตออกแล้วใส่บั๊กเดิม **ยังแดง 56 ตัว** แปลว่าตอนนี้มีเทสต์อื่นคุมเส้นทางเขียนไฟล์อยู่อีกเยอะ ไม่ได้แขวนอยู่กับไฟล์เดียว · **เจอจุดบางระหว่างวัด**: พิมพ์ชื่อคอนสแตนต์ผิดบน**บรรทัดที่เลือกกลไกล็อก** (`hasattr(msvcrt, "LK_NBLCK")`) แดงแค่ **1 ตัว** เพราะ `hasattr` คืน False เฉย ๆ แล้วตกไปสาขาอื่นเงียบ ๆ — ถ้าตกจน `_LOCK_KIND == ""` จะไม่มีล็อกข้ามโพรเซสเลย คือสภาพก่อน BUG-056 ทั้งที่โค้ดดูเหมือนมีล็อก · เพิ่ม `LockKindTests` (3 ตัว) ปักหมุดว่า เลือกกลไกได้จริง ตรงกับแพลตฟอร์ม และคอนสแตนต์ที่อ้างมีอยู่จริง → อาการเดียวกันแดง **6 ตัว** เฉพาะในไฟล์นั้น · **ข้อจำกัดที่รู้ตัว**: พิมพ์ผิดฝั่ง POSIX (`fcntl.LOCK_EX`) แดง **0 ตัว** บน Windows เพราะสาขานั้นเป็นโค้ดตาย — งาน CI `ubuntu-latest` เป็นตัวที่คุมฝั่งนั้น อย่าเชื่อผลรันเฉพาะบนเครื่องพัฒนา |
| 45 | ✅ `backend.storage()` picked S3/R2 whenever `S3_BUCKET` was set, ignoring the mode — `files` mode on a box holding `.env` handed out presigned PUT URLs for the **production** bucket, which is why the R2 keys need rotating | backend-dev | **fixed (uncommitted) 2026-09-17** · remote blobs now require `MEETING_AI_REMOTE_BLOBS=1` outside cloud mode and every path announces which store it chose · tests `test_bug_045_blob_storage_optin` (13) · ticket [BUG-045](../tickets/BUG-045-implicit-remote-blob-storage.md) |
| 55 | ✅ `store.load_detail()` cached on file mtime while four of the five detail-write paths never invalidated it — in file mode an edit could be invisible to the very next read ("I saved it and it didn't save"), intermittently | backend-dev | **fixed (uncommitted) 2026-09-17** · measured: 300 consecutive writes produce 14 distinct mtimes and 286 of them repeat the previous one, so ~95% of rapid writes were invisible to that cache · found by running the suite on #2 and #5 merged together, which neither branch's own suite does · ticket [BUG-055](../tickets/BUG-055-stale-detail-cache.md) |

| 58 | **Rotate every credential in `.env`, not just R2.** The whole file sat on `edgexpert-04dc` — a shared box running ~26 containers and a CI runner — while running `db-init` from there on 2026-09-17. Deleted, but `rm -rf` does not erase and a snapshot would have caught it | owner | 🚨 **เจอของจริงระหว่างตรวจ runbook เอง 2026-09-18: `LLM_API_KEY` ตัวที่ยังใช้อยู่ อยู่ใน git history ของ repo สาธารณะมาตั้งแต่ 2026-08-10** · คอมมิต `b6daf68` มีไฟล์สองใบที่เกิดจากพิมพ์คำสั่ง PowerShell พลาด (`.setup-worker.ps1 -WorkerToken k3zJ.txt` และ `powershell -ExecutionPolicy Bypass.txt`) ซึ่งข้างในมีคีย์เต็มค่า · ถูกลบใน `c005321` วันเดียวกันแต่ **ลบไฟล์ไม่ได้ลบ blob ออกจาก history** · ตรวจแล้วว่า**ไม่มี** `DATABASE_URL`, คีย์ R2, `VERCEL_TOKEN`, `STT_API_KEY` อยู่ในนั้น และชิ้นส่วน `k3zJ` ในชื่อไฟล์เป็นคนละ token กับ `WORKER_TOKEN` ปัจจุบัน · **ข้ออ้างเดิมใน runbook ที่ว่า “ไม่มี credential อยู่ใน git — ไม่ต้องล้าง git history” จึงผิด** แก้แล้วพร้อมตัวเลขที่วัดได้ และเพิ่มทางเลือก “หมุนอย่างเดียว” กับ “หมุนแล้วล้าง history” ให้เจ้าของเลือก (ต้องหมุนก่อนเสมอ ล้างอย่างเดียวไม่ช่วยอะไร) · tests `test_backlog_58_no_secrets_in_git` (5) เปลี่ยนข้ออ้างให้เป็นสิ่งที่รันซ้ำได้: อ่านค่าจริงจาก `.env` แล้วหาในทุกไฟล์ที่ track และใน history โดย**ไม่พิมพ์ค่าออกมาเลย แม้ตอนล้ม** (ไม่มี `.env` = ข้าม จึงไม่ทำให้ CI แดง) · กลับกลไกแล้วแดง: paste ค่าลับลงเอกสารที่ track → 1, runbook กลับไปอ้างผิด → 1 · **ตรวจเพิ่ม**: `.env` ของเครื่อง worker สะอาด มีค่าลับแค่ `LLM_API_KEY` + `WORKER_TOKEN` เวลาหมุนจึงแก้ที่เครื่องนั้นแค่ 2 ค่า · ⚠️ **การหมุนยังไม่ได้ทำและ agent ทำแทนไม่ได้** — ต้องล็อกอินคอนโซลผู้ให้บริการและกรอกค่าลับ ลำดับใน [runbook](../runbooks/rotate-credentials.md) · **`LLM_API_KEY` คือตัวเดียวที่ยืนยันแล้วว่าหลุดจริงและยังหลุดอยู่ — ทำตัวนี้ก่อน** |
| 59 | ✅ **CI แดงสุ่มบน `windows-latest`** — เทสต์ BUG-011 สองตัวอิงเวลาคงที่ 1.0/1.5 วินาที และ `_harness._start_server()` ไม่รอให้เซิร์ฟเวอร์พร้อมก่อนคืน (constructor bind+listen ไปแล้ว คอนเนกชันจึงเข้า backlog ได้ทั้งที่ยังไม่มีใคร accept) | test-engineer | **fixed 2026-09-17** · วัดแล้วว่า `main` เองล้ม 2/10 รอบด้วยเทสต์คู่เดียวกัน = ของเดิม ไม่ใช่ของ PR ใด · `_wait_until_ready()` + `raw_send_and_collect(expect=, settle=)` · เซิร์ฟเวอร์ **ตอบช้า ไม่ใช่ไม่ตอบ** (89.3s ครั้งแรกใต้โหลด แล้ว ~0.3s) จึงไม่ใช่บั๊กของแอป · ticket [BUG-059](../tickets/BUG-059-ci-flaky-body-caps-harness.md) · **วัดซ้ำ 2026-09-19 ตามที่เจ้าของสั่ง**: สวีทเต็ม 890 เทสต์ × 10 รอบ (~35 นาที) → เขียว **9/10** · เทสต์สองตัวที่ตั๋วนี้แก้ (`..._413_has_connection_close_header...`, `..._keepalive_still_yields_two_responses...`) **ไม่แดงเลยสักรอบ — การแก้ยังอยู่** · รอบที่แดงคือ `test_bug_016...test_non_json_content_type_is_rejected` ซึ่งเป็น**คนละเทสต์ คนละ error คนละกลไก** ไม่ใช่การถอยกลับของตั๋วนี้ แยกไปเป็น #75 (แก้แล้ว) และ #76 (รอเจ้าของตัดสิน) · **วัดรอบสอง 2026-09-19 (20 รอบ เก็บ output ทั้งก้อน ~70 นาที)**: เขียว **20/20** ไม่มีเทสต์ตัวไหนแดงเลย · รวมกับ CI 57 รันที่สะอาด → ขอบบนของอัตราอยู่ที่ราว **p ≤ 3.8%** (20 รอบเพียว ๆ ให้แค่ p ≤ 13.9% ซึ่งไม่ตัด 10% ที่วัดได้รอบแรกออกด้วยซ้ำ) · **ยังสรุปไม่ได้ว่าหายแล้ว** และไม่มีใครเคยเห็น traceback ของ `test_bug_016...test_non_json_content_type_is_rejected` ที่แดง 1/10 รอบเลย — **เลิกทอยลูกเต๋าต่อ** (จะสรุป p<5% ต้องเขียวติดกัน 58 รอบ ~3.4 ชม. · p<1% ต้อง 300 รอบ ~17 ชม.) ให้รอมันโผล่เองแล้วอ่าน log ของ CI ซึ่งเก็บ traceback ไว้อยู่แล้ว (หลัง #77 log จะเหลือแต่ของจริง ไม่มีเสียงรบกวน 10053 ปนอีก) |
| 60 | ✅ **waveform ของประชุมยาวเกิน 20 นาทีเป็นแท่งเท่ากันหมด** (เบราว์เซอร์ถอดไฟล์เองไม่ไหว) | backend-dev + web-dev | **fixed 2026-09-17** · `runner.audio_peaks()` ให้ ffmpeg คลายเป็น PCM โมโน 1 kHz แล้วหา RMS 64 ช่วง — ประชุม 1 ชม. ใช้ **2.81 วินาที** เก็บ **261 ไบต์** (เทียบกับเบราว์เซอร์ที่ต้องใช้แรมระดับ GB) · ผ่าน `sanitize.peaks()` เพราะเป็นค่าจาก worker ที่เชื่อไม่ได้และถูกยัดลง `style="height:N%"` · คอลัมน์ `peaks jsonb` + `alter table ... add column if not exists` ทดสอบ migration กับ PostgreSQL 16.15 จริงบนฐานที่มีข้อมูลแล้ว · ของเก่าที่ไม่มี peaks ยังถอยไปใช้เส้นทางในเบราว์เซอร์ถ้าไฟล์สั้นพอ · tests `test_backlog_60_waveform_peaks` (19 เคส) |
| 62 | ✅ **คำเตือน “ยังไม่ได้ยินเสียงเลย” ค้างถาวร และเตือนไมค์ที่ตายกลางทางไม่ได้** — เจ้าของรายงานสดระหว่างอัดจริง (2026-09-19): พูดอยู่แต่หน้าเว็บขึ้นคำเตือนที่นาที 00:42 | web-dev | **fixed 2026-09-19** · **ไล่แล้วพบว่าไฟล์เสียงไม่ได้เสียเลย** — ตัวอัดไฟล์อัดจาก สตรีมต้นทางตรง ๆ ไม่ได้ผ่าน AudioContext ที่มิเตอร์ใช้ (คอมเมนต์ใน `startRecording()` เขียนข้อนี้ไว้เองอยู่แล้ว) คำเตือนที่ผิดจึงชวนให้คนหยุดอัดทิ้งของดี · **อาการ 1 คำเตือนค้าง**: `$('#rec-warn').hidden` ถูกตั้งเป็น `false` ใน `setInterval` แต่ไม่มีใครตั้งกลับเป็น `true` เลย — กดอัดแล้วเงียบ 6 วินาทีก่อนเริ่มพูด (ปกติมาก) คำเตือนจะค้างไปจนจบ · **อาการ 2 ที่ตั๋วไม่ได้ขอแต่ร้ายแรงกว่า**: เงื่อนไขเดิมเทียบกับ `rec.peak` ซึ่งเป็นค่าสูงสุด **ตลอดกาล** พอได้ยินเสียงครั้งเดียวก็เป็นเท็จตลอดไป — **ไมค์ที่หลุดตอนนาทีที่ 5 จะอัด ความเงียบไปจนจบโดยไม่มีอะไรบอก** (`track.onended` จับได้เฉพาะแทร็กที่ตายสนิท ไม่ใช่แทร็กที่ ยังอยู่แต่ส่งความเงียบมา เช่นถูกปิดที่ระดับ OS หรือ Bluetooth สลับโปรไฟล์) · แก้ด้วย `rec.heardAt` (เวลาที่ได้ยินเสียงล่าสุด) แทนพีคตลอดกาล แล้วแยกสองข้อความ: ไม่เคยได้ยินเลยเกิน 6 วิ = ตั้งค่าผิดตั้งแต่ต้น · เคยได้ยินแล้วเงียบเกิน 45 วิ = ไมค์อาจหลุด ทั้งคู่ **หายเองเมื่อมีเสียงกลับมา** · เปิดไมค์กลับจากปิดไมค์ = เริ่มนับใหม่ · tests `test_bug_062_silent_warning_sticky` (14) **ตัดฟังก์ชันจริงจาก app.js มารันด้วย node** ไม่ใช่ assert ข้อความในไฟล์ · กลับกลไก 6 แบบแดงทุกแบบ (4/1/2/1/2/1) ของจริง 0 · **ยังไม่ได้ยืนยันกับไมค์จริง** — ตรรกะรันผ่าน node แล้ว แต่ปลายทางต้องรออัดจริงอีกรอบ |
| 63 | ✅ **จอแคบ: `<select>` ในแผ่นล่างจอกดไม่ได้** — เจ้าของรายงานพร้อมภาพหน้าจอ (2026-09-19) ในเมนู ⋯ ของหน้ารายละเอียด ปุ่มใช้ได้ปกติ แต่ “🔒 เฉพาะฉัน” กับ “.docx” กดแล้วไม่มีอะไรเกิดขึ้น | web-dev | **fixed 2026-09-19** · **ไม่ใช่ CSS อย่างที่เดาตอนแรก** — `elementFromPoint` กลางตัวเลือก ทั้งสองคืนตัวมันเอง ไม่มีอะไรบัง · ตัวการคือตัวฟัง `click` บน `.detail-actions` ที่ปิดแผ่น เมื่อคลิก**อะไรก็ได้**ข้างใน: บนมือถือการแตะ `<select>` ยิง `click` ขึ้นมา**ก่อน**ตัวเลือก จะโผล่ พอปิดแผ่น `body.sheet-open` หลุด แล้ว `.detail-actions { display: none }` ของจอแคบ เข้าแทน — `<select>` หายจาก layout ตัวเลือกจึงไม่เคยขึ้นเลย · แก้ด้วย `if (e.target.closest('select')) return;` แล้วปิดแผ่นตอน `change` ของ `#d-visibility` แทน (ฟอร์แมตดาวน์โหลดไม่ปิด เพราะต้องกดปุ่ม ⬇ ต่อในแผ่นเดียวกัน) · **ยืนยันในเบราว์เซอร์จริงที่ความกว้างมือถือ** โดยดึงตัวฟังตัวจริงจาก `app.js` ที่เสิร์ฟอยู่ มารัน: ก่อนแก้ แตะ select → “แผ่นถูกปิด · display=none” · หลังแก้ → “แผ่นยังเปิดอยู่” ส่วนปุ่มยังปิดแผ่นเหมือนเดิม · tests `test_bug_063_sheet_select_unusable` (7) รันตัวฟัง ตัวจริงด้วย node โดยปลอมเฉพาะ DOM รอบ ๆ · กลับกลไก 4 แบบแดงทุกแบบ (2/3/1/1) ของจริง 0 |
| 64 | ✅ **งานบอทที่ไม่เคยถูกรับเข้าห้อง รายงานสาเหตุผิด** — เจ้าของแจ้ง “ส่ง bot เข้าห้อง error” (2026-09-19) แต่ข้อความที่ได้คือ “ถอดเสียงไม่ได้ข้อความเลย — ไฟล์อาจไม่มีเสียงพูด” ซึ่งพาไปไล่หาปัญหาไมโครโฟน | backend-dev | **fixed 2026-09-19** · **บอททำงานถูกทุกขั้น** — log บอกว่า Meet ขึ้น “You can't join this video call … No one can join a meeting unless invited or admitted by the host … Returning to home screen in 60 seconds” บอทยืนรอ ~20 วิแล้วถูกเตะออก ได้ไฟล์เงียบ → ถอดเสียงไม่ได้ → ล้มด้วยข้อความที่ชี้ผิดทาง · **มีการ์ดดักกรณีนี้อยู่แล้วแต่ไม่ทำงาน**: `mean_volume(wav) < SILENT_DB` (-55 dB) — หน้ารอของ Meet มีเสียง UI ปนอยู่ ไฟล์จึงไม่ต่ำพอ การ์ดที่อิงความดังเป็นการ**อนุมาน** ส่วนสถานะที่คอนเทนเนอร์รายงานเอง (`bot_status.txt` → waiting/inroom/left) เป็นสัญญาณ**ตรง** · เก็บสถานะที่เคยเห็นจาก `tick()` แล้วถ้าถอดเสียงไม่ได้ **และ**ไม่เคยเห็น `inroom` ให้เปลี่ยนเป็น “บอทไม่เคยได้เข้าห้อง — ยืนรออยู่หน้าห้อง mm:ss แล้วถูกพาออก ต้องมีคนกด Admit” · **จงใจใช้สองเงื่อนไขร่วม ไม่ใช่สถานะอย่างเดียว** เพราะคอนเทนเนอร์รุ่นเก่าที่ไม่เขียน `bot_status.txt` จะได้ไม่ถูกทิ้งประชุมที่อัดมาดี ๆ (มีเทสต์คุมข้อนี้โดยเฉพาะ) · ยุบข้อความ “ถอดเสียงไม่ได้” เป็นค่าคงที่ `NO_SPEECH_ERROR` ที่เดียว · tests `test_bug_064_bot_never_admitted` (8) · กลับกลไก 4 แบบแดงทุกแบบ (3/1/1/1) ของจริง 0 |
| 65 | ✅ **บอทเสียเวลาให้ selector ที่ไม่มีอยู่ ก่อนจะเจอปุ่มจริง** — เจ้าของบอกว่า “บอทส่งเข้าช้า” | web-dev | **fixed 2026-09-19** · วัดจาก log: รับงาน→กดปุ่มเข้าห้อง 11–12 วิ โดย **5 วิหายไปช่วงตั้งชื่อ→กดเข้าห้อง** · `click_first()` ไล่ selector ทีละตัว ตัวที่ไม่มีอยู่จริง กิน timeout เต็มก้อนก่อนลองตัวถัดไป — ห้องที่ต้องขออนุมัติมีแต่ `Ask to join` จึงเสีย 5 วิให้ `Join now` ทุกครั้ง และถ้า UI เปลี่ยนจนไม่เจอเลยเสีย 4 × 5 = 20 วิ · แก้เป็นรอทุก selector พร้อมกันหนึ่งครั้งแล้วค่อยเลือกตามลำดับเดิมด้วยเวลาสั้น ๆ **ลำดับความสำคัญไม่เปลี่ยน** (สำคัญกับรายการที่มีตัวสำรองกว้าง ๆ ท้ายสุด เช่น `input[type="text"]` ของ Zoom) · กัน selector คนละ engine ไม่ให้ถูกรวมด้วยคอมมา (`text=` / `xpath=`) · tests `test_bug_065_bot_join_latency` (9) วัด “งบเวลาที่ขอรอ” ไม่ใช่เวลานาฬิกา จึงรันเร็ว · กลับกลไก 4 แบบแดงทุกแบบ (4/6/1/2) ของจริง 0 |
| 66 | ✅ **บอทออกจากห้องในวินาทีเดียวกับที่กดปุ่มเข้าห้อง** — เจ้าของส่งบอทสดแล้ว log ขึ้น `11:56:53 กดปุ่มเข้าห้องแล้ว` / `11:56:53 ตรวจพบว่าประชุมจบ/ออกจากห้องแล้ว` | web-dev | **fixed 2026-09-19** · **นี่คือต้นเหตุจริงของอาการ “บอทเข้าห้องไม่ได้” ทั้งหมดวันนี้ ไม่ใช่เรื่อง กด Admit ไม่ทัน — บอทไม่เคยให้โอกาสกดเลย** · `MEET_END` จับคำว่า `Return to home` ซึ่งเป็น ข้อความบน**หน้าห้องรอของ Meet เอง** (`Returning to home screen in 60 seconds`) และ `_monitor()` ตรวจตัวจับจบห้องตั้งแต่รอบแรก คือก่อนบอทเข้าห้องได้ · แก้: ตัวจับ “ประชุมจบ” เชื่อได้ต่อเมื่อ **เคยเข้าห้องแล้ว** เท่านั้น ช่วงก่อนหน้านั้นใช้เพดานเวลารอ `JOIN_WAIT_SEC` = 120 วิแทน — ไม่งั้นห้องที่ปฏิเสธบอทจริงจะถูกอัดความเงียบจนครบ `MAX_MINUTES` (180 นาที) · แยกเป็นฟังก์ชันล้วน `platforms.leave_reason()` จึงทดสอบได้ครบทุกทางโดยไม่ต้องมี Docker · tests `test_bug_066_bot_leaves_before_admitted` (13) รวมข้อที่ปักหมุดว่าข้อความสองชุดทับกันจริง · กลับกลไก 5 แบบแดงทุกแบบ (9/3/4/1/1) ของจริง 0 · ⚠️ **ยังไม่ได้ยืนยันกับห้องจริง** — ต้องรอ worker ดึงโค้ดแล้ว build image ใหม่ |
| 67 | ✅ **ตัวตรวจ “ล็อกอินบอทแล้วหรือยัง” ผ่านทั้งที่ยังไม่เคยล็อกอิน** — เจ้าของแจ้งว่า “ไม่มี bot ส่งถึงห้อง” (2026-09-19) | backend-dev | **fixed 2026-09-19** · **นี่คือต้นเหตุที่เหลืออยู่จริงหลังแก้ #65/#66** · log บอกว่า Meet ตอบทันทีว่า `You can't join this video call … No one can join a meeting unless invited or admitted by the host` แล้วสองนาทีต่อมา redirect ไปหน้าโฆษณา Workspace ที่มีปุ่ม “ลงชื่อเข้าใช้” — บอทเข้าห้องแบบ**ไม่ระบุตัวตน** (ขั้น `ตั้งชื่อบอท` ยืนยันอีกที: ช่องกรอกชื่อ โผล่เฉพาะตอนไม่ได้ล็อกอิน) ห้องที่ไม่รับ guest จึงปฏิเสธทันที และ **host ไม่เคยเห็นการ เคาะประตู** จึงไม่มีอะไรให้กด Admit · แต่ `missing_pieces()` ตอบว่า “ไม่ขาดอะไร” เพราะเช็กแค่ว่าโฟลเดอร์ `bot/profile` ไม่ว่าง — **วัดบนเครื่อง worker: 5 ไฟล์ 92 KB ไม่มี `Default/Cookies` เลย** คอนเทนเนอร์ที่เคยรันแล้ว ออกโดยยังไม่ล็อกอิน ทิ้งไฟล์โครงของ Chromium ไว้พอให้ผ่านการตรวจ ระบบจึงบอกว่าพร้อม แล้วไปล้มกลางห้องประชุมจริงแทน · แก้เป็น `profile_ready()` ที่ดู `Default/Cookies` ว่ามีจริงและไม่ว่าง ใช้ทั้งใน `missing_pieces()` และก่อน `docker run` ของ `join_and_record()` · tests `test_bug_067_bot_profile_not_logged_in` (8) รวมเคสจำลองสถานะจริงบนเครื่อง worker · กลับกลไก 4 แบบแดงทุกแบบ (4/1/1/1) ของจริง 0 · ⚠️ **เจ้าของต้องรัน `./mai bot-login` เอง** — agent รันไม่ได้ (ต้องกรอกรหัส Google) |
| 68 | ✅ **`mai bot-login` พังสนิทเงียบ ๆ ตั้งแต่ #21** — เจ้าของรันแล้วหน้าจอค้างรอ “กด Enter เพื่อบันทึก” ทั้งที่คอนเทนเนอร์ตายไปตั้งแต่วินาทีแรก (2026-09-19) | devops-engineer | **fixed 2026-09-19** · `docker logs maibot_login` → `❌ เขียน /out ไม่ได้ (รันในนามผู้ใช้ 1000:1000)` · `bot.login()` mount มาให้แค่ `/prof` เพราะโหมดล็อกอินไม่ได้อัดเสียง แต่ `/out` **มีอยู่จริงในอิมเมจ** จากบรรทัด `VOLUME ["/out"]` และเป็นของ root — พอ #21 เปลี่ยนไปรัน ในนาม `pwuser` การตรวจสิทธิ์เขียนที่ #21 เพิ่มเข้ามาเองจึงล้มทุกครั้ง entrypoint ออกด้วย exit 1 · **เป็นต้นตอของทุกอาการเรื่องบอทวันนี้**: ล็อกอินไม่เคยสำเร็จ → `bot/profile` ว่าง → บอทเข้าห้อง แบบไม่ระบุตัวตน → ห้องที่ไม่รับ guest ปฏิเสธ → host ไม่เคยเห็นการเคาะประตู → ผู้ใช้เห็นแค่ “ส่งบอทแล้วไม่มีอะไรเกิดขึ้น” (#64 #66 #67 ล้วนเป็นอาการปลายทางของข้อนี้) · แก้: โหมด `login` ตรวจแค่ `/prof` ส่วนโหมดอัดเสียงยังตรวจทั้ง `/out` และ `/prof` เหมือนเดิม · กันชั้นสอง: Dockerfile ยก `/out` ให้ `pwuser` ตั้งแต่ build · tests `test_bug_068_bot_login_out_preflight` (8 — 3 ตัวรันเฉพาะ POSIX ที่ไม่ใช่ root คือบน CI ubuntu) ตัดบล็อกตรวจสิทธิ์จาก entrypoint ตัวจริงมารันด้วย bash |
| 69 | ✅ **โปรไฟล์บอทที่ root เป็นเจ้าของ — ล็อกอินกี่ครั้งก็ไม่ถูกบันทึก และไม่มีอะไรฟ้อง** | devops-engineer | **fixed 2026-09-19** · เจ้าของล็อกอินสำเร็จตามขั้นตอนทุกอย่าง โปรไฟล์โต จาก 92 KB เป็น 5.0 MB (5 → 21 ไฟล์) แต่ `profile_ready()` ยังเป็น `False` · วัดจากในคอนเทนเนอร์: `drwx------ 32 root root Sep 17 10:36 Default` — โฟลเดอร์ session **มีอยู่** แต่ถูกสร้างตอนคอนเทนเนอร์ยังรันเป็น root (ก่อน #21) พอ #21 เปลี่ยนไปรันในนาม uid 1000 ทั้ง host และคอนเทนเนอร์ก็อ่าน/เขียนไม่ได้ Chromium จึงเขียนได้แค่ไฟล์ระดับบน (แคช, Local State) ส่วน session หายทุกครั้ง · `find` จากฝั่ง host ก็มองไม่เห็น `Default/` ด้วยเหตุผลเดียวกัน ทำให้ตอนแรกสรุปผิดว่า “ยังไม่เคยล็อกอิน” · แก้: `profile_problem()` แยก “ยังไม่ล็อกอิน” ออกจาก “เข้าถึงโปรไฟล์ไม่ได้” และบอกคำสั่ง `chown` ตรง ๆ — สองอย่างนี้แก้คนละทาง และการบอกให้ `bot-login` ซ้ำในกรณีหลังคือการชี้ให้ ไปทำสิ่งที่ไม่มีวันสำเร็จ · entrypoint ตรวจถึง `/prof/Default` ด้วย ไม่ใช่แค่ระดับบนของ `/prof` (การเขียน `.mai-write-test` ที่ระดับบนผ่านมาตลอด ทั้งที่โปรไฟล์ใช้ไม่ได้) · tests `test_bug_067_bot_profile_not_logged_in` (11) · ⚠️ **เจ้าของต้องรัน `sudo chown -R 1000:1000 bot/profile` บนเครื่อง worker** — ต้องใช้ root และแตะโฟลเดอร์ที่ agent ห้ามแตะ |
| 70 | ✅ **บอทอยู่ในห้องแล้วแต่ระบบมองไม่เห็น จึงเตะตัวเองออกใน 2 นาที** | web-dev | **fixed 2026-09-19** · log ตอนบอทเข้าห้องได้สำเร็จครั้งแรก: `ไม่มีช่องกรอกชื่อ — ปกติถ้า บอทล็อกอิน Google อยู่แล้ว` → `กดปุ่มเข้าห้องแล้ว` → `เฝ้าดูมา 0s (ยังไม่เข้าห้อง) — call_end / **ออกจากการโทร** / chat / แชทกับทุกคน` · หน้าเว็บมีแถบควบคุมในห้องครบ แต่ `MEET_IN` หาคำว่า **“ออกจากสาย”** ซึ่ง Meet เปลี่ยนคำแปลไทยเป็น **“ออกจากการโทร”** ไปแล้ว · **ผลหนักกว่าการรายงานสถานะผิด**: `was_inside` ไม่เคยเป็นจริง แล้วกติกา `JOIN_WAIT_SEC` ที่เพิ่งเพิ่มใน #66 ก็เตะบอทออกจากห้องที่มันเข้าได้แล้ว — ของที่เพิ่งซ่อมกลายเป็นตัวทำลาย การประชุมเสียเอง (วัดจริง: ถูกเตะที่ 02:17 พร้อมข้อความ “ไม่เคยได้เข้าห้อง” ซึ่งผิด) · แก้ `MEET_IN` ให้ครอบคำใหม่+คำเก่า+ภาษาอังกฤษ และเติม fallback กว้าง `button[aria-label*="ออกจาก"]` เพราะ Meet เปลี่ยนคำแปลมาแล้วอย่างน้อยหนึ่งครั้ง · **ยืนยันกับ DOM จริงในเบราว์เซอร์**: selector ถูกต้อง · หน้าในห้องแมตช์ 1 · หน้าห้องรอแมตช์ 0 (false positive อันตรายกว่า false negative เพราะจะรายงานว่าอยู่ในห้องทั้งที่ยังรอ) · เพิ่มกติกาว่า in-room marker ต้องเป็น CSS ล้วน ห้ามมี pseudo-class เฉพาะของ Playwright เพราะ selector ที่พาร์สไม่ผ่านจะไปโผล่กลางประชุมจริงที่เดียว · tests `test_bug_070_meet_in_room_thai_label` (10) · กลับกลไก 4 แบบแดงทุกแบบ (1/3/3/1) · **เคสที่ยังไม่เคยเห็นตอนทำตั๋วนี้ โผล่ในประชุมจริง 2026-09-19**: หน้าห้องรอของ Meet **มีคำว่า “ออกจากการโทร” อยู่ในข้อความบนหน้าด้วย** (`โปรดรอจนกว่าผู้จัดการประชุมจะนำคุณเข้าร่วม / … / call_end / ออกจากการโทร / chat`) ถ้า `MEET_IN` จับจาก **text** มันจะ false positive ตั้งแต่ยังรออยู่หน้าห้อง แล้วแบนเนอร์ Admit ของ #72 จะหายก่อนผู้ใช้ได้กด · ที่ไม่เกิดเพราะ selector จับ `aria-label`/`data-tooltip` ซึ่งปุ่มบนหน้าห้องรอไม่มี — **ห้ามเปลี่ยนไปจับข้อความบนหน้าเด็ดขาด** log จริงยืนยัน: 23:46:43 “ยังไม่เข้าห้อง” (ถูกต้อง) → 23:46:48 “เข้าห้องแล้ว” หลังเจ้าของกด Admit |
| 71 | ✅ **ส่งบอทแล้วหน้ายังเป็นฟอร์มเดิม ดูเหมือนยังไม่ได้ส่ง** — เจ้าของส่งภาพหน้าจอมา (2026-09-19) บอทเข้าห้องแล้วและนับเวลาอยู่ใน log แต่หน้าเว็บบนมือถือยังเป็นฟอร์ม “ประชุมใหม่” ที่ช่องลิงก์ถูกล้าง | web-dev | **fixed 2026-09-19** · หลังส่งสำเร็จโค้ดใส่งาน เข้า `state.jobs` แล้ว `renderJobs()` — แต่การ์ดงานอยู่ใน `.sidebar` ซึ่ง CSS จอแคบซ่อน ทั้งแถบตอน `body[data-view="new"]` ผู้ใช้จึงไม่มีทางเห็น “บอทอยู่ในห้อง mm:ss” เลย เห็นแต่ฟอร์มที่ว่าง ซึ่งอ่านได้ว่ายังไม่ได้ส่ง แล้วกดส่งซ้ำ · **วัดกับเบราว์เซอร์จริงที่ความกว้างมือถือ**: `view=new` → sidebar `display:none` การ์ดงานมองเห็น = **false** · `view=home` → `display:flex` มองเห็น = **true** · แก้ด้วย `showNew('#home')` หลังส่งสำเร็จ (บนเดสก์ท็อปฟอร์มยังอยู่ครบ ไม่เสียอะไร) และเลิก ล้าง `#b-url` เอง เพราะ `showNew()` สร้างฟอร์มใหม่ให้อยู่แล้ว · tests `test_bug_071_bot_submit_stays_on_form` (8) รวมข้อที่ปักหมุดว่าการ์ดงานอยู่ใน sidebar จริง — ถ้าวันหนึ่งย้ายออก เหตุของบั๊กนี้ก็หายไป · กลับกลไก 3 แบบแดงทุกแบบ |
| 72 | ✅ **แบนเนอร์ “ไปกด Admit” ค้างอยู่ทั้งที่บอทเข้าห้องแล้ว** — ภาพหน้าจอจากเจ้าของ (2026-09-19): การ์ดงานบอก “บอทอยู่ในห้อง 03:02” แต่แบนเนอร์เหนือมันในจอเดียวกันยังบอกให้ ไปกดรับเข้าห้อง | web-dev | **fixed 2026-09-19** · `banner()` เดิมไม่มีวันหมดอายุ ตั้งครั้งเดียว แล้วค้างจนกว่าจะมีข้อความใหม่ทับ — คำแนะนำที่ถูก ณ วินาทีที่กดส่ง จึงกลายเป็นคำสั่งที่ผิดใน อีกสิบวินาทีต่อมา และขัดกับข้อมูลที่อยู่ใต้มันเอง · แก้ด้วยการติดป้าย `tag` ว่าแบนเนอร์ขึ้นเพราะ เรื่องอะไร แล้ว `expireBanner()` ถอนออกตอน poll เจอว่า**งานของเรา** (`isMine`) รายงานว่า เข้าห้องแล้ว — กรอง `isMine` เพราะแอดมินเห็นคิวทั้งระบบ งานของ tenant อื่นต้องไม่มาถอน แบนเนอร์ของเรา (บทเรียนจาก #41) · **ข้อความ “บอทอยู่ในห้อง” ถูกสร้างที่ `runner.py` แต่ถูก อ่านที่ `app.js`** สองไฟล์คนละภาษาที่ต้องตรงกัน จึงมีเทสต์ผูกสองฝั่งไว้ ถ้าใครแก้ฝั่งเดียว จะรู้ทันที แทนที่จะปล่อยให้แบนเนอร์เงียบ ๆ เลิกหายเองโดยไม่มีใครสังเกต · tests `test_bug_072_admit_banner_stale` (9) · กลับกลไก 6 แบบแดงทุกแบบ · **เทสต์ตัวหนึ่งเจาะจงลำดับบรรทัดเกินจำเป็น** (สลับลำดับที่ยังถูกต้องแล้วแดง) แก้ให้ตรวจเฉพาะ สิ่งที่สำคัญจริงคือ “ถูกเรียกใน pollJobs” และ “อยู่หลัง state.jobs ถูกอัปเดต” · **ยืนยันกับประชุมจริงแล้ว 2026-09-19 23:46** (Meet `rhq-whkj-sib` เจ้าของกด Admit ภายใน 5 วินาที) journal ของ worker: `23:46:46 บอทรออยู่หน้าห้อง 00:20 — กด “รับเข้าห้อง” ให้บอท` → `23:46:56 บอทอยู่ในห้อง 00:30` **ข้อความที่ `runner.py` ส่งออกมาตรงกับค่า `BOT_IN_ROOM` ใน `app.js` เป๊ะ** เทสต์ที่ผูกสองไฟล์ไว้จึงยืนกับของจริงได้ · เจ้าของยืนยันว่าแบนเนอร์หายไปเองบนจอ |
| 73 | ✅ **สลับลำดับ “เอาเสียงเข้ามายังไง” — ส่งบอทซ้ายสุด อัปโหลดขวาสุด** — เจ้าของขอ (2026-09-19) บอทเป็นทางที่ใช้บ่อยที่สุดแล้ว แต่ต้องกดผ่านอีกสองแท็บกว่าจะถึง | web-dev | **done 2026-09-19** · ย้าย **สามที่พร้อมกัน**: แถบปุ่ม `#cap-seg` · การ์ดใน `.cards` (จอกว้างกางทั้งสามใบตามลำดับ DOM ถ้าย้ายแค่ปุ่ม จอกว้างกับจอแคบจะเรียงคนละแบบ) · คำโปรยบนปุ่ม “เริ่มประชุมใหม่” ของจอแคบ · **ผลพลอยได้ที่ตั้งใจ: แท็บเริ่มต้นเปลี่ยนเป็น “ส่งบอท”** เพราะ `setupCapturePicker()` เรียก `pick(btns[0].dataset.cap)` คือปุ่มซ้ายสุดที่ใช้ได้ ไม่ได้เขียนค่าเริ่มต้นตายตัวไว้ที่ไหน — ถ้าอยากให้เปิดมาเจออัปโหลดเหมือนเดิม ต้องแก้บรรทัดนั้นเป็นค่าคงที่ · ตรวจในเบราว์เซอร์จริงที่ 375x812 hit-test กลางปุ่มแล้วยิงคลิกจริงทั้งสามปุ่ม กดโดนตัวเองหมด (x=75 / 188 / 301) การ์ดที่โชว์ตรงกับปุ่มทุกครั้ง และเคสแอดมินปิดอัดสด ปุ่มที่เหลือ = bot(เลือก), upload ยังตกซ้ายสุดเหมือนเดิม · tests `test_bug_073_capture_tab_order` (9) · กลับกลไก 6 แบบแดงทุกแบบ (เรียงปุ่มกลับ 5 แดง) · **ยืนยันกับเซิร์ฟเวอร์จริง 2026-09-19** (ไม่ใช่ไฟล์ static ที่ก๊อปไปวาง): รัน `python -m meeting_ai.cli web` จาก **git worktree** พอร์ต 8790 แล้วกดปุ่ม “เริ่มประชุมใหม่” จริงบนจอ 375x812 → ปุ่มซ้ายไปขวา `ส่งบอท | อัดสด | อัปโหลดไฟล์` (`config.ROOT` มาจาก `Path(__file__)` worktree จึงมี root ของตัวเอง ไม่แตะคลังจริง และไม่มี `.env` ติดไปด้วย) |
| 74 | ✅ **เปิดหน้า “ประชุมใหม่” มาให้ตกที่แท็บ “อัดสด”** — เจ้าของขอ (2026-09-19) ทันทีหลัง #73 | web-dev | **done 2026-09-19** · แท็บเริ่มต้นเคยเป็น**ผลข้างเคียงของลำดับ** (`pick(btns[0].dataset.cap)` = ปุ่มซ้ายสุด) อยากเปลี่ยนต้องเรียงปุ่มใหม่ทั้งแถบ — พอ #73 ย้ายบอทไปซ้ายสุด แท็บเริ่มต้นก็เปลี่ยนตามไปโดยไม่มีใครขอ · แยกเป็น `DEFAULT_CAP` ต่างหาก ลำดับกับค่าเริ่มต้นจึงขยับแยกกันได้ ซึ่ง**จำเป็น** เพราะ “อัดสด” อยู่ตรงกลางแถบ เป็น `btns[0]` ไม่ได้เลย · **ต้องมีทางถอย**: แอดมินปิดการ์ดอัดสดได้ ปุ่มมันจะหายจาก `btns` ทั้งอัน ถ้าไม่ `|| btns[0]` จะเปิดมาเจอหน้าที่ไม่มีแท็บไหนถูกเลือก · ผลพ่วงที่ตั้งใจ: แถบอัดลอย (`body.cap-rec`) โผล่ตั้งแต่เปิดหน้า เพราะมันคือสิ่งที่แท็บนี้มีไว้ให้กด · วัดในเบราว์เซอร์จริง 375x812 ครบสามเคส (ปกติ / แอดมินปิดอัดสด → ถอยไป bot / เหลือทางเดียว → แถบซ่อน) และ hit-test ปุ่ม “⏺ เริ่มอัด” ที่ y=769 กดโดนตัวเอง ไม่มีอะไรบัง · tests `test_bug_074_default_capture_tab` (9) — และ **ย้ายเรื่องค่าเริ่มต้นออกจากไฟล์ของ #73** (9 → 6) ไม่ให้สองไฟล์อ้างกติกาคนละแบบ · กลับกลไก 7 แบบแดงทุกแบบ · **ยืนยันกับเซิร์ฟเวอร์จริง 2026-09-19**: เปิดหน้ามาแล้วตกที่ `อัดสด` การ์ดที่โชว์คือ `rec` และแถบอัดลอย “⏺ เริ่มอัด” โผล่พร้อมกัน ตรงตามที่ตั้งใจ |
| 75 | ✅ **`raw_request` ทำคำตอบหายเมื่อเซิร์ฟเวอร์ปฏิเสธตั้งแต่ header แล้วปิด** — เจอระหว่างวัด #59 | test-engineer | **fixed 2026-09-19** · ของเดิมส่งสอง `sendall` (header แล้วค่อย body) ระหว่างสองก้อนนั้นเซิร์ฟเวอร์อาจตอบ 400 แล้ว `close()` ทั้งที่ body ยังค้างใน receive buffer — TCP ตอบด้วย **RST ไม่ใช่ FIN** และ Windows ทิ้งไบต์ที่ฝั่งเรารับมาแล้วแต่ยังไม่ได้อ่านไปพร้อมกัน คำตอบที่มาถึงแล้วหายทั้งก้อน · **สมมติฐานแรกผิด**: คิดว่า `sendall(body)` ล้ม แต่ body 10 ไบต์ไม่เคยล้มเลย มันลงบัฟเฟอร์ในเครื่องได้เสมอ ความพังไปโผล่ที่ `recv` — ทางแก้จึงไม่ใช่ “อ่านก่อนตัดสินว่าส่งล้ม” แต่คือไม่เปิดช่องว่างนั้นตั้งแต่แรก · วัด 40 ครั้งต่อแบบ: แยกส่ง 28/40 · แยกส่งคั่น 30ms **0/40** · แยกส่งเซิร์ฟช้า 37/40 · **ส่งก้อนเดียว 40/40 ไม่มี error เลย** ทั้งตอนเซิร์ฟเวอร์ตอบไวและตอบช้า · tests `test_backlog_75_raw_request_single_send` (5) · กลับกลไก 3 แบบแดงทุกแบบ · **ข้อจำกัดที่บอกไว้ในเทสต์**: ตัว race เองทำให้เกิดตามสั่งไม่ได้ เทสต์พิสูจน์ได้แค่ว่า header+body ถึงเซิร์ฟเวอร์ใน `recv()` ครั้งเดียว ส่วนที่เหลือคือตัวเลขวัดซ้ำข้างบน |
| 76 | ✅ **ระบาย body ที่ยังไม่ได้อ่านก่อนปิด ไม่ใช่เฉพาะเส้น 413** | backend-dev | **done 2026-09-19** · เส้นที่ตอบก่อนอ่าน body (401/403/404/405/415/400 framing ผิด) ทิ้งไบต์ของ client ค้างไว้ พอปิด socket TCP ยิง RST แล้วฝั่ง client ทิ้งไบต์ที่รับมาแล้ว แต่ยังไม่ได้อ่านไปด้วย — คำตอบหายทั้งก้อน เห็นแค่ connection reset · แก้ที่ `_send()` จุดเดียว ครอบทุกเส้น ด้วยตัวนับ `body_read` แล้วระบาย**ส่วนต่าง** (เส้น 413 เลิกเรียกเอง เรียกสองครั้งเสี่ยงรอเพดานเวลาสองรอบ) · ไม่มีเพดานใหม่: `_drain_rejected_body` คิด `min(pending, LINGER_DRAIN)` อยู่แล้ว body 29 ไบต์ก็ระบาย 29 ไบต์ · **ผมวัดผิดรอบแรกแล้วเอาไปเขียนใน PR #75**: แบบจำลองแรก `close()` เฉย ๆ แต่ของจริง `socketserver.TCPServer.shutdown_request()` ทำ `shutdown(SHUT_WR)` ก่อนเสมอ ตัวเลข 25/40 → 40/40 ในตั๋ว/PR นั้นจึงใช้ไม่ได้ · **วัดใหม่ด้วยแบบจำลองที่ซื่อตรง** (240 ครั้ง): ไม่ระบาย **230/240** (ConnectionAbortedError 10) 8.3 ms/คำขอ · ระบาย **240/240** ไม่มี error 7.3 ms/คำขอ · **ข้อจำกัดที่ต้องรู้**: ยิงกับเซิร์ฟเวอร์จริง 600 ครั้ง (ก่อน/หลัง อย่างละ 300) ได้ 415 ครบทั้งคู่ และรันโมดูล #016 ซ้ำ 40 รอบขณะอัด CPU 10 โปรเซส แดง 0/40 — **ยังพิสูจน์ไม่ได้ว่าแก้แล้วเทสต์ที่แดง 1/10 รอบหายไป** อัตราจริงต่ำกว่าที่ 600 คำขอจับได้ ที่พิสูจน์ได้คือเส้นนี้ทิ้ง body ไว้จริงและการระบายปิดรูที่วัดได้โดยไม่เสียเวลาเพิ่ม · **รอบหน้าถ้ามันแดงอีก ให้เก็บ traceback ไว้** (สคริปต์วัดของผมเก็บแค่ชื่อเทสต์ จึงไม่เคยเห็น error จริงของมันเลย — นั่นคือเหตุผลที่ต้องเดา) · tests `test_backlog_76_drain_before_close` (7) · กลับกลไก 6 แบบแดงทุกแบบ · ระหว่างทางเจอว่า `if self.close_connection:` ที่ผมใส่ไว้เป็นเงื่อนไขที่จริงเสมอ (กลับกลไกแล้วไม่มีใครแดง) จึงถอดทิ้งแทนที่จะเก็บสาขาที่ทดสอบไม่ได้ · **เทสต์ของตั๋วนี้ทำ flake ขึ้นมาเอง แก้แล้ว 2026-09-20**: `test_the_unread_body_is_drained` แดง **2 ครั้งจาก ~11 รอบของสวีทเต็ม** (`[] != [29]`) แต่รันโมดูลเดี่ยว ๆ ไม่เคยแดง · สาเหตุอ่านออกจากโค้ด ไม่ใช่เดา: การระบายเกิด **หลัง** เขียนคำตอบลง socket (จำเป็น — ต้องให้ 415 ออกไปก่อน) ไคลเอนต์จึงอ่านจบและ `post_raw()` คืนค่าได้ **ก่อน** เธรดเซิร์ฟเวอร์เดินมาถึงบรรทัดระบาย พอเทสต์ออกจาก `with` แล้วถอด patch สายลับก็ไม่เห็นอะไรเลย · แก้ที่เทสต์ด้วย `threading.Event` + `spy.wait()` **ไม่แตะ `server.py`** เพราะโค้ดทำถูกมาตลอด · กลับกลไก 6 แบบยังแดงครบเหมือนเดิม · ยืนยันด้วยสวีทเต็ม 4 รอบเขียว — **4 รอบพิสูจน์ไม่ได้ว่าหายขาด** (อัตราเดิม ~18% โอกาสรอดโดยบังเอิญยังราว 45%) แต่ต่างจาก #59 ตรงที่รู้กลไกแน่ชัดแล้ว |
| 77 | ✅ **traceback ของคอนเนกชันที่ client ทิ้งไปเอง ไม่ต้องขึ้น log** | backend-dev | **done 2026-09-19** · `socketserver.BaseServer.handle_error()` พิมพ์ traceback เต็มลง stderr ทุกครั้งที่เธรดจัดการคำขอโยน exception รวมถึงตอน client ปิดไปเอง ซึ่งไม่ใช่ความผิดของเราและไม่มีอะไรให้แก้ · **วัดจาก log ของ CI จริง**: รัน `test (windows-latest)` ที่ **ผ่านทั้งรอบ** (run 35449170539) มี `ConnectionAbortedError [WinError 10053]` อยู่ **15 ครั้ง** traceback ชี้ที่ `handle_one_request()` → `rfile.readline(65537)` คือรอคำขอถัดไปบน keep-alive แล้วอีกฝั่งหายไป · **ราคาของเสียงรบกวนนี้ไม่ใช่แค่ log รก**: ระหว่างไล่ #59/#75/#76 ผมอ่านมันปนกับชื่อเทสต์แล้วสรุปว่า `test_bug_011_content_length_surrounding_whitespace_rejected` ล้มด้วย 10053 — **ไม่จริงเลย** แล้วสร้างสมมติฐาน วัด และเขียนตั๋วบนความเข้าใจผิดนั้นไปหนึ่งวัน · เงียบเฉพาะ `BrokenPipeError`/`ConnectionResetError`/`ConnectionAbortedError` ซึ่งเป็น**กลุ่มเดียวกับที่ `_route()` ดักไว้อยู่แล้ว**ว่า “เบราว์เซอร์ปิดไปกลางทาง ไม่ใช่ปัญหา” · **ไม่รวม `TimeoutError`** ทั้งที่ดูเหมือนเสียงรบกวนเหมือนกัน เพราะ timeout อาจแปลว่าเราเองช้า และไม่เคยเจอใน log จริงสักครั้ง — เติมของที่ยังไม่เคยเห็นคือการเดา · tests `test_backlog_77_quiet_client_aborts` (8) ครึ่งหนึ่งตรวจว่า **ยังพิมพ์** `ValueError`/`TimeoutError`/`OSError` อยู่ เพราะเงียบหมดคือการซ่อนบั๊กจริง ซึ่งแย่กว่า log รก · กลับกลไก 6 แบบแดงทุกแบบ (เงียบทุกชนิด 3 แดง · เงียบ OSError ทั้งตระกูล 4 แดง · เช็คผิดทาง 6 แดง) |
| 78 | ✅ **ชื่อการประชุมเป็นช่องบังคับ** — เจ้าของขอ (2026-09-20) | web-dev | **done 2026-09-20** · เดิมเว้นว่างได้แล้วระบบตั้งชื่อให้เอง (`อัดสด 19/9/2569 23:46` หรือชื่อไฟล์ที่อัปมา) คลังจึงเต็มไปด้วยชื่อที่ค้นไม่เจอภายหลัง · **เรื่องจังหวะสำคัญกว่าตัวเช็ค**: ดัก **ก่อนเริ่มงาน** ไม่ใช่ตอนส่ง — คนที่อัดไปแล้วสี่สิบนาทีแล้วเพิ่งรู้ว่าลืมใส่ชื่อ แย่กว่าการไม่มีตัวเช็คเสียอีก จึงเรียก `requireTitle()` ที่ **ต้นทางทั้งสามเส้น**: `startRecording()` ก่อนขอสิทธิ์ไมค์ · `sendBot()` ก่อนแตะลิงก์และก่อน `btn.disabled = true` (ไม่งั้นปุ่มค้าง disabled ทั้งที่ยังไม่ได้ส่ง) · `uploadFile()` ก่อน `submitMeeting()` บวกที่ปุ่ม/กล่องลากวางเพื่อไม่ให้ dialog เปิดมาเปล่า ๆ · **เก็บ `fallbackTitle` ไว้เป็นตาข่ายกันตก** ถ้าเส้นไหนหลุดมาได้ การทิ้งไฟล์ที่อัดมาสี่สิบนาทีแย่กว่าการตั้งชื่อให้เอง · `required` + `aria-required` + ป้าย “จำเป็น” ในเลเบล ผู้ใช้จะได้รู้ก่อนโดนปฏิเสธ · **วัดกับเบราว์เซอร์จริง 375x812 บนเซิร์ฟเวอร์จริงจาก git worktree** ดัก `#f-file.click`, `getUserMedia`, `window.fetch`: ชื่อว่าง → เปิด dialog 0 ครั้ง · ขอสิทธิ์ไมค์ 0 ครั้ง · ยิง `/api/meetings/bot` 0 ครั้ง · แถบอัดไม่โผล่ · ปุ่มบอทไม่ค้าง disabled · ช่องว่างล้วนถูกปฏิเสธเหมือนว่าง · มีชื่อแล้วไปต่อจริง (dialog 1 / API 1) · tests `test_backlog_78_title_required` (17) · กลับกลไก 10 แบบแดงทุกแบบ |
| 79 | ✅ **จอกว้างใช้แท็บเหมือนจอแคบ** — เจ้าของขอพร้อมภาพหน้าจอเดสก์ท็อป (2026-09-20) | web-dev | **done 2026-09-20** · เดิมจอกว้างกางการ์ดทั้งสามใบ ซึ่งกลายเป็นกริดสองคอลัมน์ที่ใบที่สามตกไปแถวล่าง เหลือที่ว่างข้าง ๆ หนึ่งช่อง และทำให้หน้ายาวขึ้นโดยไม่ได้อะไรกลับมา — คนก็เลือกทางเดียวอยู่ดี · ถอด `.seg` ออกจากแถวที่ซ่อนของจอกว้าง ย้ายกฎของแท็บออกจาก `@media (max-width: 860px)` มาไว้ระดับบนสุด และเลิกแยกทางด้วย `isMobile()` ใน `setupCapturePicker()` · **ของที่ตั้งใจไม่เปลี่ยน**: แท็บหน้ารายละเอียด (`#d-seg`) ยังเป็นของจอแคบล้วน (เจ้าของพูดถึงฟอร์มประชุมใหม่ ไม่ใช่ทั้งเว็บ) · คำอธิบายใต้หัวข้อการ์ดยังซ่อนเฉพาะจอแคบ จอกว้างมีที่พอและข้อความมีประโยชน์จริง · **กฎถอดกรอบผูกกับ `.seg:not([hidden])` ไม่ใช่กับความกว้างจอ** พอเหลือทางเดียวแถบซ่อนตัว การ์ดจึงได้ padding/border/h3 คืนเอง (วัดแล้ว 16px / 1px / block) และ `gap: 0` ก็ผูกเงื่อนไขเดียวกัน ไม่งั้นการ์ดสองใบติดกันตอนแถบซ่อน · วัดในเบราว์เซอร์จริง 1280x900: ปุ่มกว้างเท่ากัน 283px (x=367/652/938) hit-test กลางปุ่มแล้วยิงคลิกจริง โดนตัวเองทั้งสาม การ์ดตรงกับปุ่มทุกครั้ง แถบอัดลอยโผล่เฉพาะแท็บอัดสด **ความสูงทั้งหน้าเหลือ 1104px** · จอ 375x812 เหมือนเดิมทุกอย่าง · tests `test_backlog_79_capture_tabs_everywhere` (10) · กลับกลไก 8 แบบแดงทุกแบบ |
| 80 | ✅ **หน้ารายละเอียดใช้แท็บทั้งสองจอ** — เจ้าของขอต่อจาก #79 (2026-09-20) | web-dev | **done 2026-09-20** · เหตุผลที่จอแคบแยกแท็บมาตั้งแต่แรกคือ “สรุปกับบทถอดเสียงยาวมากทั้งคู่ วางต่อกันแล้วต้องเลื่อนผ่านสรุปทั้งอันกว่าจะถึงบทถอดเสียง” — ซึ่งจริงบนจอกว้างพอ ๆ กัน ความกว้างจอไม่ได้ทำให้สรุปสั้นลง · ย้าย `.dtab.tab-off` กับกฎซ่อนหัวข้อที่ซ้ำกับชื่อปุ่ม ออกจาก `@media (max-width: 860px)` มาไว้ระดับบนสุด และถอด `isMobile()` ออกจาก `setupDetailTabs()` · **วัดจริง 1280x900**: กางสองส่วนแบบเดิมหน้าสูง 2013px · แบบแท็บ 1447px **ลดลง 566px เหลือ 72%** · ปุ่ม “สรุป” x=580 / “บทถอดเสียง” x=1008 กว้างเท่ากัน 426px hit-test กลางปุ่มแล้วยิงคลิกจริง โดนตัวเองทั้งคู่ พาเนลตรงกับปุ่มทุกครั้ง · หัวข้อ h3 ที่ซ้ำกับชื่อปุ่มถูกซ่อนทั้งสองอัน แต่ **ปุ่มควบคุมในแถวหัวข้อ (`#t-edit`) ยัง hit-test ผ่าน** ไม่ได้โดนซ่อนไปด้วย · กฎจัดแถวของจอแคบ (`.section-head { flex-wrap }`, `.detail-head { flex-direction: column }`) ยังอยู่ในกรอบจอแคบตามเดิม · **แก้เทสต์ของ #79 ด้วย**: ข้อที่ปักหมุดว่า “แท็บหน้ารายละเอียดยังเป็นของจอแคบล้วน” ไม่จริงแล้ว ถอดออกแล้วย้ายความรับผิดชอบมาไฟล์นี้ ไม่ให้สองไฟล์อ้างกติกาคนละแบบ · **กับดักที่เสียเวลาไปหนึ่งรอบ**: `store.create()` รับ mid อะไรก็ได้ แต่ `_common.ID_RE` บังคับ `[0-9]{8}-[0-9]{6}-[0-9a-f]{6}` — seed ด้วย id สวย ๆ อย่าง `demo0001` จึงบันทึกได้แต่เปิดไม่ได้ (400 “id ไม่ถูกต้อง”) · tests `test_backlog_80_detail_tabs_everywhere` (10) · กลับกลไก 6 แบบแดงทุกแบบ |

## P2 — quality, debt, docs
| # | Item | Owner | Notes |
|---|---|---|---|
| 23 | ✅ CI: GitHub Actions running `compileall`, `./mai --help`, and `tests/` | devops-engineer | **done 2026-09-17 (PR #17)** · `.github/workflows/ci.yml` สาม job ไม่มี secret · [run แรกเขียวทั้งหมด](https://github.com/itsaraphapthanatka/meeting_ai/actions/runs/35204069477): ubuntu `264 tests OK (skipped=7)` 76s · windows เท่ากัน 96s · ubuntu+postgres `264 tests OK` **0 skip** 74s · ticket [BUG-023](../tickets/BUG-023-ci.md) · เหลือให้ owner: ตั้ง Branch protection → Require status checks ถ้าอยากให้บล็อกการ merge จริง |
| 24 | ✅ Dead cleanup never scheduled: `pgstore.purge_expired()`, `workers_forget()`, `db.close()`, `blobstore.reset()`, `backend.health()` → sessions/dead workers accumulate | backend-dev | **fixed 2026-09-18** · ตั๋วรวมห้าอย่างไว้ พอไล่จริงแยกได้สามกอง — **รั่วจริง**: `purge_expired()` (sessions โตหนึ่งแถวต่อการล็อกอินหนึ่งครั้งตลอดอายุระบบ) กับ `workers_forget()` (เครื่องที่ใช้ครั้งเดียวเมื่อปีที่แล้วยังค้างในรายการ) ไม่ใช่ช่องโหว่เพราะคิวรีกรอง `expires_at > now()` อยู่แล้ว แต่เป็นตารางที่ไม่มีวันหยุดโตบนฐานที่คิดค่าตามพื้นที่ · **สุขอนามัย**: `db.close()` ต่อเข้า `finally` ของ `serve()` (Neon นับ connection เป็น ทรัพยากรมีเพดาน) และ `backend.health()` ต่อเป็น `GET /api/health` (สาธารณะ แต่ `db_missing` ให้เฉพาะแอดมิน) · **ตั๋วเข้าใจผิด**: `blobstore.reset()` ไม่ใช่โค้ดตาย มันคือตะขอที่ `tests/_harness.py` เรียกอยู่สามที่ ปล่อยไว้ตามเดิม · กลไก: `store.sweep()` ตัวเดียวใช้ทั้งสองโลก — โพรเซสที่รันยาวมีเธรด `mai-sweeper` ชั่วโมงละครั้ง ส่วน serverless เกาะทาง `jobs.claim()` ที่วิ่งสม่ำเสมอ · กันซ้ำสองด่าน: ด่านในโพรเซส (ไม่แตะฐานเลย) และด่านในฐานด้วย upsert ที่มี `where` บน `do update` ซึ่งใช้ **นาฬิกาของฐาน** จึงกันหลาย instance สวีปพร้อมกันได้ · `sweep_if_due()` กลืนข้อยกเว้นเพราะเกาะอยู่บนทางหยิบงาน ล้มเมื่อไรต้องไม่ลากงานของ worker ล้มตาม · tests `test_backlog_24_cleanup_scheduled` 18 เคส (8 เคสยิง Postgres จริงใน CI job ที่มี service container) |
| 25 | ✅ Retention for `logs/` (real-meeting screenshots) and failed `recordings/bot/*.wav` | devops-engineer | **fixed 2026-09-18** · `bot.prune_old_artifacts()` ลบภาพ `logs/bot_*.png` และโฟลเดอร์พักที่กำพร้าใน `recordings/bot/` ที่เก่าเกิน `BOT_RETENTION_DAYS` (ค่าเริ่มต้น 30 วัน, 0 = เก็บตลอดไป) · worker รันบนเธรด `mai-bot-pruner` (daemon) ทำทันทีหนึ่งรอบแล้ววันละครั้ง · **รอบแรกวางไว้บนลูปรับงานแล้ว CI ฝั่ง Windows จับได้**: prune ถาม docker ซึ่งรอได้ถึง `DOCKER_TIMEOUT` แล้วเดินไล่โฟลเดอร์ทั้งชุด ทำให้ตอนสั่งปิดชนเพดาน drain ของ #20 (เทสต์นั้นล้มจริง 4.17s > 3.0s) เก็บกวาดจึงต้องไม่คั่นทางหยิบงาน · **นี่ไม่ใช่แค่เรื่องพื้นที่ดิสก์**: ภาพเห็นชื่อผู้เข้าร่วม/แชท/สไลด์ ส่วนโฟลเดอร์พักที่รอดจาก `_prune_stages()` คือ wav ที่ย้ายไม่สำเร็จ = เสียงประชุมจริงที่อาจ ไม่มีสำเนาที่อื่น · ด้วยเหตุนั้นจึงมีสองด่านก่อนลบเสียง: เก่าเกินกำหนด **และ** ไม่ใช่ของ container ที่ยังรันอยู่ (ถาม docker ทุก worker บนเครื่อง ไม่ใช่เฉพาะของเรา) ถามไม่ได้ = ไม่แตะโฟลเดอร์พักเลย แต่ภาพยังลบได้เพราะไม่ใช่ไฟล์ที่ใครกำลังเขียน · ดูอายุจากไฟล์ข้างในด้วย ไม่ใช่แค่ mtime ของโฟลเดอร์ (ซึ่งไม่ขยับตอนไฟล์ถูกเขียนทับ) · ลบเฉพาะไฟล์ที่ชื่อขึ้นต้นตาม `SHOTS` — `worker.log` และไฟล์อื่นใน `logs/` ห้ามแตะ · พิมพ์บอกทุกครั้งที่ลบโฟลเดอร์เสียง ไม่ให้หายเงียบ ๆ · tests `test_backlog_25_retention` 19 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 5 |
| 25b | ✅ `logs/worker.log` โตไม่มีที่สิ้นสุด — `worker-service.ps1` redirect stdout เข้าไฟล์เดียว ด้วย append ไม่มีการ rotate เครื่องที่รันมานานหลายเดือนจะได้ไฟล์ log ขนาดใหญ่ที่เปิดอ่านไม่ไหว | devops-engineer | **fixed 2026-09-18 (พร้อม #28)** · **วัดก่อน: การหมุนมีอยู่แล้ว แต่เช็คขนาดครั้งเดียวก่อนเข้าไปป์ไลน์** — ตั้งเพดาน 0 MB รันรอบเดียว ได้ log **890,994 ไบต์ โดยไม่หมุนสักครั้ง** · เหตุคือ `Out-File -Append` ถือ handle ไว้จนจบไปป์ไลน์ และ worker ที่ไม่พังจะไม่รีสตาร์ต เลยหลายเดือน ไฟล์จึงโตได้ไม่จำกัดตลอดอายุโพรเซส · เปลี่ยนเป็น `Write-Log` ที่นับไบต์เองแล้วหมุนระหว่างทาง (`Add-Content` เปิด-ปิดไฟล์ ทุกบรรทัด จึงเปลี่ยนชื่อไฟล์ได้โดยไม่มี handle ค้าง) เก็บย้อนหลัง `-MaxLogFiles` รุ่น (ตั้งต้น 3) แทน `.old` ใบเดียว · เทสต์รันตัวห่อจริงกับเอาต์พุต 300 บรรทัด แล้วตรวจว่าไฟล์หลักเล็กจริงและมี `.1`/`.2`/`.3` ครบ |
| 26 | ✅ `store.py` / `pgstore.py` share ~70 duplicated lines (`new_id`, `valid_id`, `fmt_time`, `transcript_text`, `_snippet`) | backend-dev | **fixed 2026-09-18** · `web/_common.py` เก็บไว้ชุดเดียว ทั้งสองโมดูล re-export ชื่อเดิม ผู้เรียก (`server.py`, `exports.py`, เทสต์) จึงไม่ต้องแก้ · ลดโค้ด 88 บรรทัดเหลือ 20 · **ตั๋วเขียนเหมือนเป็นเรื่องความสวยงาม แต่ของจริงมันเพี้ยนไปแล้ว และเพี้ยนไปทางเดียว**: ฝั่ง Postgres ถูกทำให้ทนค่า None ไปแล้วสามจุด ฝั่งไฟล์ไม่ถูกแก้ตาม วัดจริงก่อนรวม — `fmt_time(None)` → TypeError, `transcript_text` ที่ text=None → AttributeError, `_snippet(None,…)` → AttributeError ส่วนฝั่ง cloud คืน "00:00"/""/"" มานานแล้ว · กับดักที่ปล่อยให้ None หลุดเข้ามา: `meta.get("duration", 0)` คืน 0 เฉพาะตอน**ไม่มีคีย์** ถ้าคีย์มีแต่ค่าเป็น null มันคืน None · ผู้เรียกคือหน้ารายละเอียดกับตัว export พังตรงนั้น = เปิดการประชุมนั้นไม่ได้เลย · **เกือบพลาดตอนรวมสองอย่าง** และเทสต์คุมไว้ทั้งคู่: เขียน `fmt_time` เป็น HH:MM:SS เสมอ (ของจริงคืน MM:SS เมื่อไม่ถึงชั่วโมง = เปลี่ยนเวลาที่ผู้ใช้เห็น ทุกอัน) และเผลอเขียน `new_id` ใหม่ด้วย `random.randrange` แทน `secrets.token_hex(3)` · tests `test_backlog_26_shared_helpers` 19 เคส เทียบผลสองโหมดทุกอินพุต · พิสูจน์ไม่ vacuous: เอา store.py ฉบับก่อนรวมกลับมา → ล้ม 8 |
| 27 | ✅ Delete root `meeting-ai-poster.html` (byte-identical to `web/static/`); add `bot/` to `.vercelignore` | devops-engineer | **fixed 2026-09-18** · ยืนยันด้วย `cmp` ว่าเหมือนกัน ทุกไบต์ (22,385) และไม่มีโค้ดไหนอ้างถึง — ตัวที่เสิร์ฟจริงคือใต้ `server.STATIC_DIR` · เพิ่ม `bot/` ใน `.vercelignore` (บอทรันใน Docker บนเครื่อง worker ฝั่ง Vercel เรียกไม่ได้อยู่แล้ว) |
| 28 | ✅ `meeting-ai-worker.service` / `*.ps1` hardcode prod URL and username; `worker-service.ps1` builds a script without quote escaping | devops-engineer | **fixed 2026-09-18** · ทำคู่กับ #25b เพราะอยู่ไฟล์เดียวกัน · **วัดก่อนแก้: ตั๋วเขียนไว้เบากว่าความจริง** — `-Name "x'; <คำสั่ง>; '"` ไม่ได้แค่ทำให้ไฟล์พัง แต่ได้ไฟล์ที่**พาร์สผ่าน 0 error แล้วรันคำสั่งนั้นจริง** (พิสูจน์ด้วยการให้มันสร้างไฟล์) · ชื่อธรรมดาอย่าง `O'Brien's PC` ทำให้ worker ได้ `--name O` บวกอาร์กิวเมนต์ขยะอีกสองตัว โดยไม่มีอะไรเตือน · แก้ด้วย `Q()` ที่เปลี่ยน `'` → `''` ก่อนฝังทุกค่า (root, python, api, name) · `meeting-ai-worker.service` ถูกแทนด้วย `meeting-ai-worker.service.in` + `install-worker-service.sh` ที่เติมชื่อผู้ใช้/กลุ่ม/HOME/พาธของเครื่องนั้นเอง ส่วน `MAI_API`/`MAI_MAX_BOTS` มาจาก `EnvironmentFile=-/etc/default/meeting-ai-worker` · URL ของ production หายออกจาก `worker-service.ps1` และ `setup-worker.ps1` ด้วย · **กับดักที่เจอระหว่างเขียนตัวติดตั้งเอง: bash 5.2 ตีความ `&` ในฝั่งแทนที่ของ `${s//pat/rep}` ว่าเป็น “ข้อความที่แมตช์”** โฟลเดอร์ `/srv/a&b` จึงกลายเป็น `/srv/a@ROOT@b` เงียบ ๆ (วัดบน bash 5.2.37) — จึงตัดสตริงเองแทน · tests `test_backlog_28_25b_worker_service` (18) · **CI จับสองข้อที่เครื่องนี้จับไม่ได้**: (1) รัน --install แบบไม่ใช่ root แล้ว systemctl ตอบ “Interactive authentication required” ทำให้ set -e ฆ่าสคริปต์ทิ้งทั้งที่เขียน unit ไปแล้ว (2) ubuntu มีแต่ `pwsh` ไม่มี `powershell` เทสต์การหมุน log จึงเขียวทั้งที่ตัวห่อ ไม่ได้เอาต์พุตอะไรเลย — แก้ทั้งสองแล้วและเพิ่มการ์ดกันกลวง ตัดตรรกะจากไฟล์จริงมารัน **ไม่เคยเรียก `install` ตรง ๆ** เพราะมันจะลงทะเบียน scheduled task จริงบนเครื่องที่รันเทสต์ · ⚠️ **เจ้าของต้องลงมือเอง**: บน GB10 รัน `sudo ./install-worker-service.sh --install --api <url>` แล้ว `systemctl daemon-reload && systemctl restart meeting-ai-worker` · บนเครื่อง Windows รัน `.\worker-service.ps1 install` ใหม่ (`run-worker-hidden.ps1` ถูก gitignore ไว้ ตัวเก่าบนเครื่องจึงยังไม่มีของใหม่จนกว่าจะติดตั้งซ้ำ) · ยังยืนยันไม่ได้: ไม่มี systemd ให้ทดสอบ unit จริง |
| 29 | ✅ Docs drift: poster platform table reversed vs README; README says 5-min Vercel limit vs `maxDuration 60`; README tree lists `join_meet.py`, omits 7 modules | owner | **fixed 2026-09-18** · **ข้อที่หนักสุดไม่ใช่ "เอกสารไม่ตรง" แต่เป็นการโฆษณาสิ่งที่ไม่มี**: โปสเตอร์ ติดป้าย Zoom ว่า "รองรับ · ล็อกอินบอท" และ Teams ว่า "กำลังพัฒนา" ซึ่งกลับด้านกับความจริง (Zoom บล็อกบอทตามนโยบาย และโปรเจกต์ตั้งใจไม่หลบเลี่ยง ส่วน Teams ใช้ได้จริง — `bot/platforms.py` มี `join_teams`) แก้ทั้งสี่จุดในโปสเตอร์ · README เคยมีสองตารางในไฟล์เดียวกันที่ขัดกันเอง ตอนนี้เหลือข้อความชุดเดียว · เพดาน Vercel 5 นาที → 60 วินาทีตาม `vercel.json` (ผิดไป 5 เท่า) · ผังไฟล์: `join_meet.py` → `join_meeting.py` + เพิ่ม `platforms.py`/`login.py` และโมดูลที่ขาดอีก 9 ตัว |
| 30 | ✅ Add `LICENSE`; add `CLAUDE.md` capturing the conventions in PROJECT-CONTEXT | owner / devops-engineer | **fixed 2026-09-18** · **LICENSE เป็นการตัดสินใจของเจ้าของ ไม่ใช่ ของเอเจนต์** — ถามแล้วได้คำตอบว่า "ไม่ให้ใครเอาไปใช้" จึงเขียนเป็นข้อสงวนสิทธิ์ (ไทย+อังกฤษ) ไม่ใช่สัญญาอนุญาตแบบโอเพนซอร์ส · เหตุผลที่ต้องมีไฟล์ทั้งที่ "ไม่มีไฟล์" ก็แปลว่าสงวนสิทธิ์อยู่แล้ว: คนทั่วไปเห็น repo สาธารณะแล้วเข้าใจว่าใช้ได้ฟรี ไฟล์นี้จึงบอกตรง ๆ ว่า public บน GitHub ไม่ได้ให้สิทธิ์ใช้งาน และบอกทางติดต่อขออนุญาต · แยกส่วนของผู้อื่น (whisper.cpp, Playwright, ffmpeg, โมเดล ggml) ออกชัดเจนว่าอยู่ใต้สัญญาของตัวเอง · `CLAUDE.md` เป็น "กฎที่ห้ามแหก" ฉบับสั้น (< 120 บรรทัด เพราะโหลดเข้าบริบททุกครั้ง) ชี้ไป PROJECT-CONTEXT/BACKLOG แทนการคัดลอก · เก็บกฎที่มาจากเหตุการณ์จริงของโปรเจกต์นี้ ไม่ใช่คำแนะนำ ทั่วไป และเก็บวิธีทำงานที่ได้ผลจริงไว้ด้วย (วัดก่อนเชื่อ · เทสต์ต้องพิสูจน์ว่าไม่ vacuous · เทสต์ของที่ shipped ไม่ใช่สำเนา · UI ต้อง hit-test) · tests `test_backlog_30_license_and_claude_md` 16 เคส รวมด่านที่จับได้ถ้ามีใครเผลอวางข้อความ MIT/Apache/GPL ลงไป (ลอง → ล้ม 6) และด่านที่ตรวจว่า ทุกลิงก์ใน CLAUDE.md ชี้ไปไฟล์ที่มีอยู่จริง |
| 30b | ✅ `docs/PROJECT-CONTEXT.md` มีย่อหน้าซ้ำที่ขัดกันเอง | owner / devops-engineer | **fixed 2026-09-18** · ของจริงซ้ำ **ห้าจุด** ไม่ใช่สองอย่างที่คิดตอนแรก: Storage 3 ชุด, Auth pattern 3 ชุด, P1 2 ชุด, Baseline 3 ชุด · **ยุบแบบ "เอาชุดล่าสุด" จะทำข้อมูลหาย** — วัดแล้วพบว่าแต่ละชุดมีประโยคที่อีกชุดไม่มี: `rate_limits` ในรายชื่อตารางอยู่แค่ชุดที่สอง, ไฟล์ล็อกข้ามโพรเซส (BUG-056) อยู่แค่ชุดแรก, `allow_remote` (BUG-045) อยู่แค่ชุดที่สาม, บล็อก rate limit ทั้งก้อน (BUG-010, 7 ประโยค) อยู่แค่ชุด Auth ที่สอง จึงรวมแบบ union แล้วยืนยันทุกข้อกับโค้ดจริงก่อนเก็บ (ตาราง 9 ตัวใน schema.sql, `_store_lock_path`, `get_storage(allow_remote=)`, `claim_first_admin`) · แก้สถานะที่ขัดกันเองด้วย: สามที่เขียนว่า `uncommitted` ทั้งที่อยู่ใน main แล้ว (ยืนยันด้วย `git log -S`: `0710d5b`, `c0b1115`) และ สแนปช็อตจำนวนเทสต์สามรุ่นซ้อนกัน (64/76/81) ที่ล้าสมัยทั้งหมด · 111 → 104 บรรทัด แต่เนื้อหาที่ไม่ซ้ำอยู่ครบ · tests `test_backlog_30b_context_not_duplicated` 7 เคส กันไม่ให้โตแบบเดิมอีก (ข้ามบล็อกโค้ด เพราะคำสั่งคนละอันขึ้นต้นเหมือนกันได้) และผูก รายชื่อตารางไว้กับ `schema.sql` จริง · พิสูจน์ไม่ vacuous: เอาไฟล์เดิมกลับมา → ล้ม 9 |
| 31 | ✅ `diarize.py` loads whole WAV as `list[float]` (GBs for 2 h) and O(segments×turns) labelling | backend-dev | **fixed 2026-09-18** · **วัดจริงก่อนแก้ด้วย tracemalloc**: 32.8 ไบต์ต่อ sample → 3.78 GB สำหรับประชุม 2 ชม. ที่ 16 kHz (float ของ Python = ออบเจกต์ 24 ไบต์ + พอยน์เตอร์ในลิสต์อีก 8) · ตอนนี้อ่านเป็นบัฟเฟอร์ numpy float32 ทีละก้อน = 4.0 ไบต์ต่อ sample → 0.46 GB (ลด 8.2 เท่า) และ sherpa-onnx รับ float32 อยู่แล้วตามตัวอย่างของมันเอง · `<i2` ระบุ little-endian ตรงสเปก WAV แทนการฝากไว้กับ byte order ของเครื่อง · มีทาง `array.array("f")` เผื่อเครื่องที่ไม่มี numpy (อ่านไฟล์ได้ แต่ทางที่ diarize จริงต้องมี sherpa ซึ่งลาก numpy มาด้วยเสมอ) · `label_segments()` เดินสองตัวชี้พร้อมกัน — 400×400 เคยดู turn 160,000 ครั้ง เหลือ 400 · **กับดักที่เกือบพลาด**: segment ที่อยู่ในหลาย turn พร้อมกัน (คนพูดทับกัน) ทุก turn ทับเท่ากับ ความยาว segment **เป๊ะ ๆ** ของเดิมให้ตัวแรกในลำดับที่รับมาชนะ เรียงใหม่โดยไม่คุมจุดนี้ = ผลเปลี่ยนเงียบ ๆ วัดได้ต่างกัน 84 ใน 300 ชุดสุ่ม จึงพก index เดิมไว้ตัดสินตอนเสมอ · ทางที่หา "คนที่ใกล้สุด" ยังสแกนทั้งรายการโดยตั้งใจ (ระยะไม่ได้เพิ่มตาม start จะตัดด้วยหน้าต่าง ไม่ได้โดยไม่เปลี่ยนคำตอบ) · ยืนยันกับ sherpa-onnx 1.13.4 ตัวจริงว่า `process()` รับบัฟเฟอร์ใหม่ได้ · tests `test_backlog_31_diarize_memory` 16 เคส รวมเทียบผลกับอัลกอริทึมเดิม 300 ชุดสุ่ม (turn ซ้อนกันและลำดับไม่เรียง) · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 4 |
| 32 | ✅ `runner.audio_duration` hardcodes `"ffprobe"` instead of deriving from `config.ffmpeg_bin` → duration 0 when ffmpeg is not on PATH | backend-dev | **fixed 2026-09-18** · `config.ffprobe_bin()` เดาตัวที่ "คู่กับ" ffmpeg ที่ตั้งไว้ (โฟลเดอร์เดียวกัน นามสกุลเดิม) และมี `FFPROBE_BIN` ให้ตั้งเองถ้าสองตัวไม่ได้อยู่ด้วยกัน · อาการเดิมร้ายตรงที่มัน **เงียบ**: `audio_duration` จับ OSError แล้วคืน 0.0 ความยาวประชุม ขึ้นเป็น 0 ทั้งที่ไฟล์ดีทุกอย่าง · เทสต์เดินทั้งแพ็กเกจด้วย AST เพื่อกันไม่ให้มีใครเขียน `"ffprobe"` ลอย ๆ กลับมาอีก |
| 33 | ✅ Duplicated constants: `stale_after: 75` (`server.py` vs `pgstore.WORKER_STALE_SECONDS`), `DEFAULT_MAX_BOTS` (`worker.py` vs `cli.py`) | backend-dev | **fixed 2026-09-18** · ย้ายไปนิยามที่ `config.py` ที่เดียว (`WORKER_STALE_SECONDS`, `WORKER_HEARTBEAT_SECONDS`, `DEFAULT_MAX_BOTS`) แล้วให้ pgstore/server/worker/cli อ่านจากที่นั่น · เลข 75 เคยอยู่ **สามที่** (pgstore ที่ SQL ใช้จริง, server ที่ตอบกลับไปบอก worker, คอมเมนต์ใน worker.py) แก้ไม่ครบ = ตัดคนที่ยังเต้นอยู่ทิ้ง หรือเก็บคนที่ตายแล้วไว้ · เทสต์ไม่ได้เช็คแค่ "ค่าเท่ากัน" (75 == 75 ผ่านได้ทั้งที่ยังก๊อปกันอยู่) แต่เช็คว่าไฟล์ปลายทางไม่มี literal เหลือ และเปลี่ยนต้นทางแล้วปลายทางเปลี่ยนตาม · tests `test_backlog_32_33_shared_constants` 15 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 6 |
| 34 | ✅ `server.py` mangled whitespace; `runner._transcribe_track` return annotation says 2-tuple, returns 3 | backend-dev | **fixed 2026-09-18** · บรรทัดต่อที่หายไปทำให้เหลือ `"stop"` ตามด้วยช่องว่าง 17 ตัวแล้ว `and` (เจอสองจุด: เส้น `/api/jobs/{id}/stop` และ ตอนตั้งชื่อประชุมของบอท) · `_transcribe_track` ประกาศ `tuple[list[dict], str]` แต่ `return` สามค่า ผู้เรียกแกะสามค่าอยู่แล้วจึงไม่พัง — เป็นกับดักของคนอ่านครั้งหน้า · เทสต์ตรวจด้วย AST ว่าจำนวนที่ประกาศตรงกับที่ `return` จริง และกวาดทั้งแพ็กเกจหาลายเซ็นของ บรรทัดต่อที่หาย |
| 35 | ✅ Replace `print()` with `logging` in `meeting_ai/` (keep emoji-style user output in CLI only) | backend-dev | **fixed 2026-09-18** · `meeting_ai/log.py` — ไลบรารีขอ logger ของโมดูลตัวเอง ส่วนการตั้งค่าอยู่ที่ขอบนอกสุด (`cli.main`) ที่เดียว · แปลง 76 จุดใน 8 โมดูล เหลือ `print()` เฉพาะ `cli.py`/`pipeline.py` ตามที่ตั๋วสั่ง · **ข้อความและปลายทางเหมือนเดิมเป๊ะ ๆ** — INFO→stdout, WARNING+→stderr, รูปแบบเป็นข้อความล้วน ไม่มีคำนำหน้า เพราะเจ้าของอ่าน `logs/worker.log` อยู่ทุกวัน การเปลี่ยนรูปต้องเป็นการตัดสินใจ ไม่ใช่ผลข้างเคียงของการย้ายบ้าน (`setup(stamp=True)` เปิดเวลานำหน้าได้เมื่อต้องการ) · **ผลพลอยได้ที่เป็นเนื้อแท้ของตั๋ว**: `logging` จับข้อยกเว้นของ handler เอง บรรทัด log จึงไม่มีทาง ล้มงานที่เรียกมันอีก — ทางสำรอง ascii ใน `stt._notice()`/`blobstore._notice()` ที่มีไว้กัน `UnicodeEncodeError` บนคอนโซล cp874 (BUG-045) กลายเป็นโค้ดตาย ลบออกทั้งคู่ · `MAI_LOG_LEVEL` ตั้ง DEBUG ได้โดยไม่ต้องแก้โค้ด · tests `test_backlog_35_logging` 15 เคส · พิสูจน์ไม่ vacuous: stash การแก้ → ล้ม 10 |
| 36 | ✅ `render.py` in the agent-team skill: pass `encoding="utf-8"` to all file I/O and normalize `relpath` to `/` for Windows | owner (skill repo) | **fixed 2026-09-18 ที่ repo ของสกิล** `itsaraphapthanatka/claude-agent-team` — [PR #2](https://github.com/itsaraphapthanatka/claude-agent-team/pull/2) merge แล้ว CI เขียวทั้ง ubuntu และ windows · **วัดแล้วหนักกว่าที่ตั๋วเขียน**: บนเครื่องที่ `locale.getpreferredencoding(False)` เป็น `cp874` render.py **อ่านเทมเพลตของตัวเองไม่ได้** — `UnicodeDecodeError: 'charmap' codec can't decode byte 0x81` ที่ `templates/agents/architect.md` แล้วตายตั้งแต่บทบาทแรก ไม่ใช่แค่ข้อความเพี้ยน · ทางแก้เดิมคือต้องจำตั้ง `PYTHONUTF8=1` ทุกครั้ง · ใส่ `encoding="utf-8"` ครบทุกจุด (8 แห่ง) และอ่าน config ด้วย `read_text` แทน `json.load(open())` · `P()` แปลง backslash เป็น `/` แล้ว **รวมสาขา `absolute_paths`** ซึ่งเดิมข้าม relpath จึงข้ามการแปลงไปด้วย และ `MEMORY_ROOT` ก็ผ่าน `P()` แล้ว · repo นั้นเดิมไม่มีเทสต์และไม่มี CI เลย — เพิ่มทั้งสองอย่าง เทสต์ encoding รัน render.py ใต้ `-X warn_default_encoding -W error::EncodingWarning` ซึ่งทำให้การเรียกไฟล์ที่ไม่ระบุ encoding กลายเป็น error **ทุกระบบปฏิบัติการ** อาการที่เจอเฉพาะบน Windows จึงจับได้บน runner ที่เป็น UTF-8 ด้วย (CI จงใจไม่ตั้ง PYTHONUTF8) · กลับกลไก 5 แบบแดงทุกแบบ (8/7/8/7/1) ของจริง 0 · ยืนยันปลายทางด้วย config จริงของโปรเจกต์นี้โดยไม่ตั้ง PYTHONUTF8: **เรนเดอร์ 22 ไฟล์ ไม่มีพาธ backslash เลย อักขระไทยรอดมา 182 ตัว** · แถวนี้เคยมี 5 คอลัมน์ในตาราง 4 คอลัมน์ แก้ให้แล้วด้วย |
| 40 | ✅ `_fail_reason` puts the worker's absolute path and 12 raw container log lines (may include the meeting link) into `jobs.error`, now visible to the owner/admin via `/api/jobs/{id}` | backend-dev | **fixed 2026-09-18** · ข้อความที่ขึ้นเว็บเหลือ "อาการ + สิ่งที่ทำต่อได้ + รหัสอ้างอิง 6 หลัก" ส่วน log ดิบกับ path ออกทาง stderr ของ worker พร้อมรหัสเดียวกัน คนดูแลเครื่องไล่ต่อได้เหมือนเดิม · hints ที่ช่วยผู้ใช้จริง (Admit / บอทถูกตรวจจับ / สถานะล่าสุด) ยังอยู่ครบ เพราะเป็นข้อความคงที่ที่เราเขียนเอง ไม่ได้เอาบรรทัด log มาต่อ · ข้อความตอน `shutil.move` ล้มก็เลิกบอก path แต่ยังบอกว่า "เสียงยังไม่หาย" · เพิ่ม `worker.public_error()` เป็นด่านสุดท้าย ลบ path ของเครื่อง (Windows/POSIX) และตัดความยาวที่ 600 ตัวอักษร ก่อนส่งเข้า `jobs.error` — ข้อยกเว้นที่เราไม่ได้เขียนเอง (FileNotFoundError, ffmpeg, shutil) พา path ขึ้นเว็บได้เองโดยไม่มีใครตั้งใจ · ระวัง URL: `https://` มี `s:` อยู่ข้างใน ตัวกรองที่หยาบไปจะกินลิงก์ทั้งเส้น มีเทสต์คุมไว้ · tests `test_backlog_40_error_leak` 17 เคส · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 6/17 |
| 41 | ✅ Admin `_job_scope()` returns `{}` (system-wide jobs) but `store.search(user_id=admin)` lists only own meetings → `pollJobs()` opens another tenant's meeting and shows a 403 banner | web-dev | **fixed 2026-09-18** · เซิร์ฟเวอร์ติดธง `mine` ให้**แอดมินเท่านั้น** ผ่าน `_jobs_view()` — แยก "เปิดดูได้ไหม" (`_may_read_job`, แอดมินได้ทุกงาน) ออกจาก "เป็นงานของฉันไหม" (`_job_is_mine`, ไม่นับสิทธิ์แอดมิน) · คนทั่วไปไม่ได้ฟิลด์นี้เลย หน้าเว็บอ่านว่า "ไม่มีฟิลด์ = ของฉัน" จึงเข้ากันได้กับเซิร์ฟเวอร์/หน้าเว็บคนละรุ่น (ใช้ `=== true` เมื่อไหร่ ผู้ใช้ทั่วไปจะกลายเป็น "ไม่ใช่ของฉัน" ทั้งหมด — มีเทสต์คุมไว้) · `pollJobs()` ให้เฉพาะงานของเราเปลี่ยนหน้าจอ/ขึ้นแบนเนอร์ได้ คิวระบบยังแสดงครบพร้อมป้าย "คิวระบบ" · ผลพลอยได้: แอดมินเคยถูก poll ทุก 1.5 วินาทีตลอดเวลาที่ "มีใครสักคนในระบบ" มีงานเดินอยู่ ตอนนี้คิวระบบล้วน ๆ poll ทุก 10 วินาที · tests `test_backlog_41_admin_system_queue` 12 เคส (รวมตัวตรวจฝั่งหน้าเว็บ — ธงที่ส่งไปแล้วไม่มีใครอ่านก็ไม่ได้แก้อะไร) · พิสูจน์ไม่ vacuous: ย้อนกลไกกลับ → ล้ม 6/12 |
| 43 | Accepted risk (documented): draft/job endpoints answer 404 before 403, so a logged-in user can confirm a foreign draft/job id exists (audit #6). `/api/meetings/{id}` answers 403 uniformly | — | revisit if ids ever become guessable |

## Ideas (not yet assessed)
- Zoom Meeting SDK integration as the supported path for Zoom recording (README lists it as not done)
- Server → browser SSE for partial summary rendering (today the browser polls every 1.5 s)
- add via `/prd <idea>`; product-manager prioritises
