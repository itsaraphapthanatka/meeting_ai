# Runbook — หมุนกุญแจหลัง `.env` หลุดออกนอกเครื่องพัฒนา

**ทำโดย:** เจ้าของระบบเท่านั้น (ต้องล็อกอินผู้ให้บริการและกรอกค่าลับ — agent ทำแทนไม่ได้)
**อัปเดต 2026-09-18** — เดิมเอกสารนี้ครอบคลุมแค่กุญแจ R2 ตอนนี้มีสามเหตุการณ์ และเหตุการณ์ที่สาม **แก้ข้ออ้างเดิมของเอกสารนี้เองที่ว่า “ไม่มี credential อยู่ใน git”** ซึ่งไม่จริง

## สามเหตุการณ์ที่ทำให้ต้องหมุน

**1. presigned URL ของบัคเก็ต production หลุดลง log (2026-09-16)**
โหมด `files` ออก presigned PUT URL ของบัคเก็ตจริงเพราะ `backend.storage()` เลือก S3 ทันทีที่เจอ `S3_BUCKET` โดยไม่สนโหมด URL ที่เซ็นแล้ว (เขียนบัคเก็ตได้ อายุ 1 ชม.) ถูกพิมพ์ลง log ของเซสชัน agent
→ ปิดช่องแล้วใน BACKLOG #45 (ต้องตั้ง `MEETING_AI_REMOTE_BLOBS=1` เอง)
กระทบ: **คีย์ R2 เท่านั้น**

**2. ไฟล์ `.env` ทั้งไฟล์เคยอยู่บน `edgexpert-04dc` (2026-09-17)**
ระหว่างเตรียมรัน `./mai db-init` จากเครื่องนั้น มีไฟล์ `.env` อยู่ที่ `~/meeting_ai-dbinit/.env` ลบไปแล้วพร้อมทั้งโฟลเดอร์ แต่:
- `edgexpert-04dc` ไม่ใช่เครื่องส่วนตัว — รันแอปอื่นราว 26 container และมี `actions-runner` อยู่ด้วย
- `rm -rf` ปลด reference ไม่ได้ล้างข้อมูลบนดิสก์
- ถ้ามี snapshot หรือ backup ของเครื่องในช่วงที่ไฟล์อยู่ ค่าจะติดไปด้วย

กระทบ: **ทุกค่าใน `.env`** ไม่ใช่แค่ R2

**3. `LLM_API_KEY` อยู่ใน git history ของ repo สาธารณะตั้งแต่ 2026-08-10** 🚨

พบตอนตรวจข้ออ้างของเอกสารนี้เอง (BACKLOG #58, 2026-09-18) คอมมิต `b6daf68`
มีไฟล์สองใบที่เกิดจากพิมพ์คำสั่ง PowerShell พลาด แล้วเชลล์สร้างไฟล์ตามชื่อคำสั่ง:

- `.setup-worker.ps1 -WorkerToken k3zJ.txt`
- `powershell -ExecutionPolicy Bypass.txt`

ทั้งสองไฟล์มี **`LLM_API_KEY` ตัวที่ยังใช้อยู่ทุกวันนี้ แบบเต็มค่า** ถูกลบไปใน `c005321`
วันเดียวกัน (`chore: Remove sensitive setup files`) แต่ **การลบไฟล์ไม่ได้ลบ blob ออกจาก
history** — ใครก็ตามที่ clone repo นี้ยังดึงค่าออกมาได้ และ repo นี้เปิดสาธารณะ

ตรวจแล้วว่า**ไม่มี**อยู่ในสองไฟล์นั้น: `DATABASE_URL`, คีย์ R2, `VERCEL_TOKEN`, `STT_API_KEY`
ส่วนชิ้นส่วน `k3zJ` ในชื่อไฟล์เป็น token คนละตัวกับ `WORKER_TOKEN` ปัจจุบัน

กระทบ: **`LLM_API_KEY` ต้องถือว่าเป็นค่าสาธารณะมาตั้งแต่ 2026-08-10** — หมุนก่อนเป็นอันดับต้น
และต้องตัดสินใจเรื่องล้าง history (ดูท้ายเอกสาร)

## สิ่งที่อยู่ใน `.env` และแต่ละตัวทำอะไรได้ถ้าหลุด

| ตัวแปร | ถ้าคนอื่นได้ไป |
|---|---|
| `DATABASE_URL` | อ่าน/เขียน/ลบฐานข้อมูล production ทั้งหมด — บทถอดเสียง สรุป บัญชีผู้ใช้ |
| `S3_ACCESS_KEY_ID` · `S3_SECRET_ACCESS_KEY` | อ่าน/เขียน/ลบไฟล์เสียงทั้งบัคเก็ต |
| `VERCEL_TOKEN` | deploy ทับโปรเจกต์ อ่าน/แก้ env vars ซึ่งหมายถึงได้ค่าอื่นทั้งหมดต่อ |
| `WORKER_TOKEN` | เรียก `/api/worker/*` ได้ทั้งชุด รวมอัปโหลดผลลัพธ์ปลอม |
| `LLM_API_KEY` · `STT_API_KEY` | ใช้โควตาของคุณ คิดเงินเข้าบัญชีคุณ |

## ลำดับการหมุน

เรียงตามความเสียหายถ้าถูกใช้ก่อน · **`LLM_API_KEY` (ข้อ 5) เป็นตัวเดียวที่ยืนยันแล้วว่าหลุดจริงและยังหลุดอยู่** ถ้าจะทำทีละตัว ให้ทำตัวนั้นก่อนเลย ที่เหลือเป็นการหมุนเชิงป้องกันจากเหตุการณ์ที่ 1 และ 2

**1. `VERCEL_TOKEN`** — หมุนก่อนเพื่อน เพราะตัวนี้เป็นกุญแจไปสู่ตัวอื่นทั้งหมด (อ่าน env vars ของโปรเจกต์ได้)
Vercel → Account Settings → Tokens → เพิกถอนตัวเก่า สร้างใหม่ → อัปเดต `.env`

**2. `DATABASE_URL`** — Neon/Supabase → รีเซ็ตรหัสผ่านของ role หรือสร้าง role ใหม่
อัปเดตที่ **Vercel env vars** แล้ว **redeploy** จากนั้นอัปเดต `.env`
ตรวจว่าเว็บยังล็อกอินและเปิดรายการประชุมได้ ก่อนเพิกถอนของเก่า

**3. คีย์ R2** — Cloudflare → R2 → Manage API Tokens → สร้างใหม่แบบ **Object Read & Write จำกัดเฉพาะบัคเก็ตที่ใช้**
อัปเดต Vercel → **redeploy** → ทดสอบอัปโหลดจริงหนึ่งรายการ → แล้วค่อยเพิกถอนตัวเก่า
⚠️ ถ้าเพิกถอนก่อนทดสอบ ระบบอัปโหลดจะล่มทั้งตัว

**4. `WORKER_TOKEN`** — เป็นค่าที่คุณกำหนดเอง ตั้งใหม่ให้ตรงกันทั้งสองฝั่ง
Vercel env vars **และ** `.env` ของทุกเครื่อง worker (1346 และเครื่องอื่นถ้ามี) แล้วรีสตาร์ต worker
worker กับเซิร์ฟเวอร์ต้องตรงกัน ไม่งั้นงานจะค้างคิว

**5. `LLM_API_KEY` · `STT_API_KEY`** — หมุนที่ผู้ให้บริการ อัปเดต Vercel + `.env` + เครื่อง worker

## หลังหมุนเสร็จ ตรวจสามอย่าง

1. **บัคเก็ต R2** — มีไฟล์แปลกปลอมช่วง 2026-09-16 เป็นต้นมาไหม
2. **ฐานข้อมูล** — บัญชีผู้ใช้ที่ไม่รู้จัก หรือ session ที่ไม่ควรมี
3. **Vercel** — deployment ที่ไม่ได้มาจากคุณ

## กันไม่ให้เกิดซ้ำ

- **อย่าคัดลอก `.env` ไปเครื่องที่ใช้ร่วมกับคนอื่นหรือกับ CI runner** ถ้าจำเป็นต้องรันคำสั่งที่ใช้ credential บนเครื่องอื่น ให้ป้อนเฉพาะค่าที่ต้องใช้ผ่าน `read -rs` (ไม่เข้า shell history ไม่เขียนลงดิสก์ หายไปเมื่อปิด session) แทนการยกไฟล์ไปทั้งไฟล์
- **แยกไฟล์ตามหน้าที่** — `.env` ของ worker ไม่ต้องมี `DATABASE_URL`, คีย์ R2 หรือ `VERCEL_TOKEN` เลย (`setup-worker.ps1` เขียนไว้ถูกแล้วว่า worker ไม่ต้องใช้)
- **แยก bucket และฐานข้อมูล dev ออกจาก production** เพื่อให้การทดสอบที่พลาดไม่แตะของจริง
- ทุกครั้งที่เปิดเซิร์ฟเวอร์ทดสอบ ให้ล้าง `S3_BUCKET S3_ENDPOINT S3_ACCESS_KEY_ID S3_SECRET_ACCESS_KEY` ก่อน (`tests/_harness.py` ล้างให้อัตโนมัติแล้ว)

## สถานะของ git history — วัดเมื่อ 2026-09-18

ตัวเลขทุกตัวข้างล่างมาจากการรันจริง ไม่ใช่การอนุมาน และ `tests/test_backlog_58_no_secrets_in_git.py`
รันซ้ำให้อัตโนมัติทุกครั้งที่มี `.env` อยู่ (บน CI ไม่มี `.env` จึงข้าม)

| ข้อ | ผล |
|---|---|
| `.env` เคยถูก track ไหม | **ไม่เคย** (อยู่ใน `.gitignore` บรรทัดที่ 2) |
| ไฟล์ตระกูล `.env*` ที่เคยเข้า git | มีแค่ `.env.example` |
| ค่าลับใน `.env.example` ทุกรุ่น (12 รุ่น) | `LLM_API_KEY` **ไม่ว่าง** แต่เป็นค่าตัวอย่าง (`sk-` + อักขระซ้ำ ๆ) ไม่ตรงกับคีย์จริง — เอกสารเดิมเขียนว่า “ว่างเปล่า” ซึ่งไม่ตรง |
| ไฟล์ที่ track อยู่ตอนนี้มีค่าลับไหม | **ไม่มี** (เทียบกับค่าจริงทั้ง 7 ตัวตรง ๆ) |
| ค่าลับที่โผล่ใน history | **`LLM_API_KEY` 1 ตัว** ใน 1 คอมมิต (`b6daf68`) — ดูเหตุการณ์ที่ 3 |
| ค่าลับอื่นใน history | `DATABASE_URL`, `STT_API_KEY`, `WORKER_TOKEN`, `VERCEL_TOKEN`, คีย์ R2 → **0 คอมมิต** |

ตรวจซ้ำเองได้ (ไม่พิมพ์ค่าออกมา):

```bash
PYTHONIOENCODING=utf-8 python -m unittest tests.test_backlog_58_no_secrets_in_git -v
```

### ต้องล้าง history ไหม

**เอกสารเดิมบอกว่าไม่ต้อง ซึ่งผิด** ตัดสินใจได้สองทาง:

1. **หมุน `LLM_API_KEY` อย่างเดียว ไม่ล้าง history** — ค่าที่หลุดกลายเป็นขยะทันทีที่หมุน
   ง่ายและไม่ทำลายอะไร แต่คีย์เก่ายังอ่านได้ตลอดไป และใครที่เก็บ clone ไว้ก่อนหน้าก็ยังมี
   **ทางนี้พอ ถ้าหมุนแล้วจริง**
2. **หมุนแล้วล้าง history ด้วย** (`git filter-repo` หรือ BFG) — ต้อง force-push ซึ่งเปลี่ยน
   hash ของทุกคอมมิตหลังจุดนั้น clone/fork/worktree ทุกตัวต้อง reset ใหม่ และ PR ที่เปิดค้าง
   จะพัง **ทำก็ต่อเมื่อหมุนแล้วและยังต้องการให้ repo สะอาดจริง ๆ**

ไม่ว่าเลือกทางไหน **ต้องหมุนก่อนเสมอ** — การล้าง history โดยไม่หมุน ไม่ได้ช่วยอะไรเลย

## สิ่งที่ตรวจแล้วและไม่ต้องกังวล

- ไม่มีค่าลับ hardcode ในโค้ด ทุกจุดอ่านจาก `os.environ` (ตรวจด้วยการเทียบกับค่าจริงทั้ง 7 ตัว
  กับทุกไฟล์ที่ git track อยู่ — ไม่พบ)
- CORS ของบัคเก็ตเป็นคนละเรื่องกับกุญแจ ไม่ต้องแก้
- **`.env` ของเครื่อง worker สะอาด** — ตรวจบน `edgexpert-1346` เมื่อ 2026-09-18: มีค่าลับแค่
  `LLM_API_KEY` กับ `WORKER_TOKEN` ไม่มี `DATABASE_URL`, คีย์ R2 หรือ `VERCEL_TOKEN` เลย
  ตรงกับที่เอกสารนี้แนะนำไว้ → เวลาหมุน ต้องไปแก้ที่เครื่องนั้นแค่ **2 ค่า**
- ตัวแปรอีก 14 ตัวใน `.env` (`FFMPEG_BIN`, `WHISPER_*`, `STT_MODEL`, `S3_REGION`,
  `MIC_DEVICE`, ...) ไม่ใช่ค่าลับ เป็นพาธ/ชื่อโมเดล/หมายเลขอุปกรณ์ ไม่ต้องหมุน
